---
import { cn } from '@/lib/utils'

interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<div
  class={cn(
    'file-tree not-prose bg-muted/20 text-foreground my-6 rounded-lg border p-4 font-mono text-sm',
    className,
  )}
>
  <slot />
</div>
