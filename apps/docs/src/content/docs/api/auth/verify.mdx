---
title: '<route>POST</route> Verify a token'
description: '<route>POST /api/[v2,v1]/auth/verify</route>'
---

<Aside>
<Tabs items={['V2', 'V1']}>
  <Tab>

```json frame="terminal"
$ <red>curl</red> <dim>-sS</dim> <white>"https://rctf-new-dev.es3n1n.io/api/v2/auth/verify"</white> \
  <dim>--json</dim> '{
    "verifyToken": "<dim><verify-token></dim>"
  }'
```

<Tabs
  items={[
    '<response>200 goodVerify</response>',
    '<response>200 goodEmailSet</response>',
    '<response>200 goodRegisterV2</response>',
    '<response>400 badJson</response>',
    '<response>400 badBody</response>',
    '<response>401 badTokenVerification</response>',
    '<response>403 badEmailChangeDivision</response>',
    '<response>404 badUnknownUser</response>',
    '<response>409 badKnownEmail</response>',
    '<response>409 badKnownName</response>',
    '<response>409 badKnownCtftimeId</response>',
  ]}
>
  <Tab>

```json
{
  "kind": "goodVerify",
  "message": "The email was verified.",
  "data": {
    "authToken": "<dim><auth-token></dim>"
  }
}
```

  </Tab>

  <Tab>

```json
{
  "kind": "goodEmailSet",
  "message": "The email was successfully updated."
}
```

  </Tab>

  <Tab>

```json
{
  "kind": "goodRegisterV2",
  "message": "The user was created.",
  "data": {
    "authToken": "<dim><auth-token></dim>",
    "teamToken": "<dim><team-token></dim>"
  }
}
```

  </Tab>

  <Tab>

```json
{
  "kind": "badJson",
  "message": "The request JSON body is malformed."
}
```

  </Tab>

  <Tab>

```json
{
  "kind": "badBody",
  "message": "The request body does not meet requirements.",
  "data": {
    "reason": "body:verifyToken: Invalid input"
  }
}
```

  </Tab>

  <Tab>

```json
{
  "kind": "badTokenVerification",
  "message": "The token provided is invalid."
}
```

  </Tab>

  <Tab>

```json
{
  "kind": "badEmailChangeDivision",
  "message": "You are not allowed to stay in your division with this email."
}
```

  </Tab>

  <Tab>

```json
{
  "kind": "badUnknownUser",
  "message": "The user does not exist."
}
```

  </Tab>

  <Tab>

```json
{
  "kind": "badKnownEmail",
  "message": "An account with this email already exists."
}
```

  </Tab>

  <Tab>

```json
{
  "kind": "badKnownName",
  "message": "An account with this name already exists."
}
```

  </Tab>

  <Tab>

```json
{
  "kind": "badKnownCtftimeId",
  "message": "An account with this CTFtime ID already exists."
}
```

  </Tab>
</Tabs>

  </Tab>

  <Tab>

```json frame="terminal"
$ <red>curl</red> <dim>-sS</dim> <white>"https://rctf-dev.es3n1n.io/api/v1/auth/verify"</white> \
  <dim>--json</dim> '{
    "verifyToken": "<dim><verify-token></dim>"
  }'
```

<Tabs
  items={[
    '<response>200 goodVerify</response>',
    '<response>200 goodEmailSet</response>',
    '<response>200 goodRegister</response>',
    '<response>400 badJson</response>',
    '<response>400 badBody</response>',
    '<response>401 badTokenVerification</response>',
    '<response>403 badEmailChangeDivision</response>',
    '<response>404 badUnknownUser</response>',
    '<response>409 badKnownEmail</response>',
    '<response>409 badKnownName</response>',
    '<response>409 badKnownCtftimeId</response>',
  ]}
>
  <Tab>

```json
{
  "kind": "goodVerify",
  "message": "The email was verified.",
  "data": {
    "authToken": "<dim><auth-token></dim>"
  }
}
```

  </Tab>

  <Tab>

```json
{
  "kind": "goodEmailSet",
  "message": "The email was successfully updated."
}
```

  </Tab>

  <Tab>

```json
{
  "kind": "goodRegister",
  "message": "The user was created.",
  "data": {
    "authToken": "<dim><auth-token></dim>"
  }
}
```

  </Tab>

  <Tab>

```json
{
  "kind": "badJson",
  "message": "The request JSON body is malformed."
}
```

  </Tab>

  <Tab>

```json
{
  "kind": "badBody",
  "message": "The request body does not meet requirements.",
  "data": {
    "reason": "body:verifyToken: Invalid input"
  }
}
```

  </Tab>

  <Tab>

```json
{
  "kind": "badTokenVerification",
  "message": "The token provided is invalid."
}
```

  </Tab>

  <Tab>

```json
{
  "kind": "badEmailChangeDivision",
  "message": "You are not allowed to stay in your division with this email."
}
```

  </Tab>

  <Tab>

```json
{
  "kind": "badUnknownUser",
  "message": "The user does not exist."
}
```

  </Tab>

  <Tab>

```json
{
  "kind": "badKnownEmail",
  "message": "An account with this email already exists."
}
```

  </Tab>

  <Tab>

```json
{
  "kind": "badKnownName",
  "message": "An account with this name already exists."
}
```

  </Tab>

  <Tab>

```json
{
  "kind": "badKnownCtftimeId",
  "message": "An account with this CTFtime ID already exists."
}
```

  </Tab>
</Tabs>

  </Tab>
</Tabs>
</Aside>

Consumes a verification-style token and turns it into the next account state. The same endpoint handles pending registrations, account recovery with a team token, and email-change confirmations.

Use the V2 route for new clients. It returns the wider registration response when a pending registration is completed.

<RouteMeta auth="Public" gate="None" />

| Field         | Type          | Required | Notes                                        |
| ------------- | ------------- | -------- | -------------------------------------------- |
| `verifyToken` | `string{:ts}` | Yes      | Pending-registration, team, or verify token. |

<Tabs items={['V2', 'V1']}>
  <Tab>

`<route>POST /api/v2/auth/verify</route>` can complete a pending registration, mint an auth token from a team token, or confirm an email change.

| Token kind | Success kind | Effect |
| --- | --- | --- |
| Pending registration | `<response>200 goodRegisterV2</response>` | Creates the team, returns both tokens, and claims the verification row. |
| Team | `<response>200 goodVerify</response>` | Returns a fresh `authToken`. |
| Verify | `<response>200 goodEmailSet</response>` | Updates the team email after re-checking division ACLs. |

For pending registrations, the response body matches the immediate-success shape from [register a team](/api/auth/register/). Team tokens are recovery or login credentials, so they only return a fresh `authToken`. Email-change tokens do not return data after the email is updated.

  </Tab>

  <Tab>

`<route>POST /api/v1/auth/verify</route>` can complete a pending registration, mint an auth token from a team token, or confirm an email change.

| Token kind | Success kind | Effect |
| --- | --- | --- |
| Pending registration | `<response>200 goodRegister</response>` | Creates the team, returns `authToken`, and claims the verification row. |
| Team | `<response>200 goodVerify</response>` | Returns a fresh `authToken`. |
| Verify | `<response>200 goodEmailSet</response>` | Updates the team email after re-checking division ACLs. |

Pending-registration and team-token flows both return a fresh auth token. Email-change tokens do not return data after the email is updated.

  </Tab>
</Tabs>

Verification can fail even when the token decrypts correctly. Verify tokens are one-time and time-limited, and email-change tokens still re-check division ACLs and duplicate-email constraints at the moment they are consumed.
