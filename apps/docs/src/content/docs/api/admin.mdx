---
title: Admin
description: Challenge management, uploads, teams, verifications, submissions, settings, and admin bot service routes.
---

Admin routes require a user auth token with the listed permission bits, except for admin bot service routes, which use the shared admin bot bearer token. Routes are grouped by resource. Permissions, captcha actions, and rate limits referenced in property tables are defined on the [API overview](/api).

## Permissions

| Permission | Required by |
| --- | --- |
| `challsRead{:ts}` | Reading admin challenge data, reading upload state, reading instancer schema, reading admin bot status, and reading admin user detail with solve history. |
| `challsWrite{:ts}` | Updating challenges and uploading files. |
| `challsSolveWrite{:ts}` | Deleting solves. |
| `usersWrite{:ts}` | Listing teams, filtering teams, mutating teams, generating team tokens, managing pending verifications, and reading submission audit logs. |
| `settingsWrite{:ts}` | Reading and updating runtime settings. |

Routes that list multiple permission bits require all of them.

## Routes

| Method | Path | Version | Auth | Gate |
| --- | --- | --- | --- | --- |
| `GET{:http}` | `/api/v2/admin/challs` | V2 | Required (`challsRead{:ts}`) | None |
| `GET{:http}` | `/api/v2/admin/challs/:id` | V2 | Required (`challsRead{:ts}`) | None |
| `PUT{:http}` | `/api/v2/admin/challs/:id` | V2 | Required (`challsWrite{:ts}`) | None |
| `DELETE{:http}` | `/api/v2/admin/challs/:challengeId/solves/:userId` | V2 | Required (`challsSolveWrite{:ts}`) | None |
| `POST{:http}` | `/api/v2/admin/upload` | V2 | Required (`challsWrite{:ts}`) | None |
| `POST{:http}` | `/api/v2/admin/upload/query` | V2 | Required (`challsRead{:ts}`) | None |
| `GET{:http}` | `/api/v2/admin/users` | V2 | Required (`usersWrite{:ts}`) | None |
| `POST{:http}` | `/api/v2/admin/users` | V2 | Required (`usersWrite{:ts}`) | None |
| `GET{:http}` | `/api/v2/admin/users/:id` | V2 | Required (`usersWrite{:ts}`, `challsRead{:ts}`) | None |
| `PUT{:http}` | `/api/v2/admin/users/:id` | V2 | Required (`usersWrite{:ts}`) | None |
| `DELETE{:http}` | `/api/v2/admin/users/:id` | V2 | Required (`usersWrite{:ts}`) | None |
| `POST{:http}` | `/api/v2/admin/users/:id/token` | V2 | Required (`usersWrite{:ts}`) | None |
| `GET{:http}` | `/api/v2/admin/user-verifications` | V2 | Required (`usersWrite{:ts}`) | None |
| `POST{:http}` | `/api/v2/admin/user-verifications/:id/complete` | V2 | Required (`usersWrite{:ts}`) | None |
| `POST{:http}` | `/api/v2/admin/user-verifications/:id/resend` | V2 | Required (`usersWrite{:ts}`) | None |
| `GET{:http}` | `/api/v2/admin/submissions` | V2 | Required (`usersWrite{:ts}`, `challsRead{:ts}`) | None |
| `POST{:http}` | `/api/v2/admin/submissions` | V2 | Required (`usersWrite{:ts}`, `challsRead{:ts}`) | None |
| `GET{:http}` | `/api/v2/admin/settings` | V2 | Required (`settingsWrite{:ts}`) | None |
| `PUT{:http}` | `/api/v2/admin/settings` | V2 | Required (`settingsWrite{:ts}`) | None |
| `GET{:http}` | `/api/v2/admin/instancer/schema` | V2 | Required (`challsRead{:ts}`) | None |
| `GET{:http}` | `/api/v2/admin/admin-bot/status` | V2 | Required (`challsRead{:ts}`) | None |
| `POST{:http}` | `/api/v2/admin/admin-bot/jobs/pull` | V2 | Service | None |
| `GET{:http}` | `/api/v2/admin/admin-bot/challenges/:id/source` | V2 | Service | None |
| `POST{:http}` | `/api/v2/admin/admin-bot/jobs/:id/complete` | V2 | Service | None |
| `POST{:http}` | `/api/v2/admin/admin-bot/jobs/:id/fail` | V2 | Service | None |
| `GET{:http}` | `/api/v2/admin/admin-bot/queue-depth` | V2 | Service | None |
| `GET{:http}` | `/api/v1/admin/challs` | V1 | Required (`challsRead{:ts}`) | None |
| `PUT{:http}` | `/api/v1/admin/challs/:id` | V1 | Required (`challsWrite{:ts}`) | None |
| `DELETE{:http}` | `/api/v1/admin/challs/:id` | V1 | Required (`challsWrite{:ts}`) | None |
| `POST{:http}` | `/api/v1/admin/upload` | V1 | Required (`challsWrite{:ts}`) | None |
| `POST{:http}` | `/api/v1/admin/upload/query` | V1 | Required (`challsRead{:ts}`) | None |

## Challenge routes

### `GET /api/v2/admin/challs`

Lists every challenge with full admin fields, including hidden challenges and unreleased challenges.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `challsRead{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Response**

`goodAdminChallenges` (200): array of `AdminChallengeV2`. See [`AdminChallengeV2`](#adminchallengev2) below.

**Errors**: `badToken`, `badPerms`.

### `GET /api/v2/admin/challs/:id`

Returns one admin challenge.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `challsRead{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Path params**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `id` | `string{:ts}` | Yes | Challenge ID. |

**Response**

`goodAdminChallenge` (200): `AdminChallengeV2` object. See [`AdminChallengeV2`](#adminchallengev2).

**Errors**: `badToken`, `badPerms`, `badChallenge`.

### `PUT /api/v2/admin/challs/:id`

Creates or updates a challenge. Omitted fields in `data` keep their current value. Setting `instancerConfig{:ts}` or `adminBotConfig{:ts}` to `null{:ts}` clears the integration config.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `challsWrite{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Path params**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `id` | `string{:ts}` | Yes | Challenge ID. Created if not present. |

**Request body**

```json
{
  "data": {
    "name": "web demo",
    "category": "web",
    "author": "organizers",
    "description": "Solve it.",
    "flag": "flag{example}",
    "points": { "min": 100, "max": 500 },
    "tiebreakEligible": true,
    "files": [{ "name": "dist.zip", "url": "/uploads/dist.zip", "size": 12345 }],
    "sortWeight": 10,
    "instancerConfig": null,
    "adminBotConfig": null,
    "hidden": false,
    "releaseTime": null
  }
}
```

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `data` | `Partial<AdminChallengeV2>{:ts}` | Yes | Partial challenge data. Omitted fields keep existing values. |

`instancerConfig{:ts}` is validated against the active instancer provider schema. `adminBotConfig{:ts}` source is validated by the active admin bot provider and stored with a generated revision. Updating a challenge triggers a leaderboard recalculation.

**Response**

`goodChallengeUpdate` (200): updated `AdminChallengeV2`.

**Errors**: `badBody`, `badToken`, `badPerms`, `badInstancerConfig`, `badAdminBotConfig`.

### `DELETE /api/v2/admin/challs/:challengeId/solves/:userId`

Deletes one solve for a specific user and challenge. Triggers a leaderboard recalculation.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `challsSolveWrite{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Path params**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `challengeId` | `string{:ts}` | Yes | Challenge ID. |
| `userId` | `string{:ts}` | Yes | Team ID whose solve is being removed. |

**Response**

`goodChallengeSolveDelete` (200, no data).

**Errors**: `badToken`, `badPerms`, `badUnknownSolve`.

## Upload routes

### `POST /api/v2/admin/upload`

Uploads one or more files. Files are deduplicated by SHA-256 — uploading the same content twice returns the existing URL.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `challsWrite{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Request**

`multipart/form-data` with one or more `files` form fields.

**Response**

`goodFilesUpload` (200): array of upload objects.

| Field | Type | Notes |
| --- | --- | --- |
| `name` | `string{:ts}` | Display filename. |
| `url` | `string{:ts}` | Public download URL. |
| `size` | `number \| null{:ts}` | File size in bytes. |

**Errors**: `badBody`, `badToken`, `badPerms`, `badFilesUpload`.

### `POST /api/v2/admin/upload/query`

Checks whether files already exist by SHA-256 without uploading them.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `challsRead{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Request body**

```json
{
  "uploads": [
    { "sha256": "<hex-digest>", "name": "dist.zip" }
  ]
}
```

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `uploads[].sha256` | `string{:ts}` | Yes | Hex-encoded SHA-256 digest. |
| `uploads[].name` | `string{:ts}` | Yes | Display filename. |

**Response**

`goodUploadsQuery` (200): array with one entry per query — each entry has nullable `url` and nullable `size`. A `null{:ts}` URL means the file is not yet uploaded.

**Errors**: `badBody`, `badToken`, `badPerms`.

## Team routes

### `GET /api/v2/admin/users`

Lists teams. Query parameters control pagination, sorting, and search.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `usersWrite{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Query**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `limit` | `number{:ts}` | Yes | Integer from `1` through `100`. |
| `offset` | `number{:ts}` | Yes | Integer ≥ `0`. |
| `search` | `string{:ts}` | No | 1–100 characters. |
| `sortBy` | `AdminTeamSortBy{:ts}` | No | `createdAt`, `team`, `email`, `division`, `score`, `solves`, or `status`. |
| `sortOrder` | `SortOrder{:ts}` | No | `asc` or `desc`. |

**Response**

`goodAdminUsers` (200): `{ total: number, users: AdminUserListItem[] }`. Each list item includes ID, name, email, division, perms, banned state, score, solve count, avatar URL, country code, status text, and `createdAt`.

**Errors**: `badBody`, `badToken`, `badPerms`.

### `POST /api/v2/admin/users`

Lists teams with rich filters. Same query parameters as `GET`, with filter object in the body.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `usersWrite{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Query**

Identical to `GET /api/v2/admin/users`.

**Request body**

```json
{
  "status": { "include": ["active"], "exclude": ["banned"] },
  "division": { "include": ["open"] }
}
```

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `status` | `Filter<AdminTeamStatus>{:ts}` | No | `active`, `banned`, `admin`. |
| `division` | `Filter<string>{:ts}` | No | Division keys. |

Filter objects accept nullable `include` and `exclude` arrays.

**Response**

Same shape as `GET /api/v2/admin/users`.

**Errors**: `badBody`, `badToken`, `badPerms`.

### `GET /api/v2/admin/users/:id`

Returns full team details, including solve history.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `usersWrite{:ts}`, `challsRead{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Path params**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `id` | `string{:ts}` | Yes | Team ID. |

**Response**

`goodAdminUser` (200):

| Field | Type | Notes |
| --- | --- | --- |
| `id` | `string{:ts}` | Team ID. |
| `name` | `string{:ts}` | Team name. |
| `email` | `string \| null{:ts}` | Team email. |
| `division` | `string{:ts}` | Division key. |
| `perms` | `number{:ts}` | Permission bitmask. |
| `banned` | `boolean{:ts}` | Whether the team is banned. |
| `score` | `number{:ts}` | Cached current score. |
| `solveCount` | `number{:ts}` | Cached solve count. |
| `avatarUrl` | `string \| null{:ts}` | Avatar URL. |
| `countryCode` | `string \| null{:ts}` | Country code. |
| `statusText` | `string \| null{:ts}` | Status text. |
| `createdAt` | `string{:ts}` | ISO timestamp. |
| `solves[]` | `{ challengeId, challengeName, challengeCategory, createdAt }[]{:ts}` | Solve history. |

**Errors**: `badToken`, `badPerms`, `badUnknownUser`.

### `PUT /api/v2/admin/users/:id`

Mutates team state. Admins cannot mutate users with admin permissions.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `usersWrite{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Path params**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `id` | `string{:ts}` | Yes | Team ID. |

**Request body**

```json
{
  "data": { "banned": true }
}
```

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `data.banned` | `boolean{:ts}` | No | Banning removes the team from leaderboard output after recalculation without deleting solves. Unbanning restores rank after recalculation. |

**Response**

`goodAdminUserUpdate` (200, no data).

**Errors**: `badBody`, `badToken`, `badPerms`, `badUnknownUser`, `badUserPrivileged`.

### `DELETE /api/v2/admin/users/:id`

Deletes a team. Cannot delete users with admin permissions.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `usersWrite{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Path params**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `id` | `string{:ts}` | Yes | Team ID. |

**Response**

`goodAdminUserDelete` (200, no data).

**Errors**: `badToken`, `badPerms`, `badUnknownUser`, `badUserPrivileged`.

### `POST /api/v2/admin/users/:id/token`

Generates a fresh long-lived team token. Useful for recovering teams that have lost access.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `usersWrite{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Path params**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `id` | `string{:ts}` | Yes | Team ID. |

**Response**

`goodCreateUserToken` (200): `{ token: string }`.

**Errors**: `badToken`, `badPerms`, `badUnknownUser`, `badUserPrivileged`.

## Pending verification routes

Pending verification rows represent users who registered with an email but have not yet completed email verification.

### `GET /api/v2/admin/user-verifications`

Lists outstanding pending verifications.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `usersWrite{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Response**

`goodAdminUserVerifications` (200): `{ verifications: PendingUserVerification[] }`. Each row contains `id`, `name`, `email`, `division`, `createdAt`, and `expiresAt`.

**Errors**: `badToken`, `badPerms`.

### `POST /api/v2/admin/user-verifications/:id/complete`

Manually completes a pending verification, creating the team without requiring the user to follow the verification email link.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `usersWrite{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Path params**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `id` | `string{:ts}` | Yes | Pending verification ID. |

**Response**

`goodAdminUserVerificationComplete` (200): `{ userId: string }`.

**Errors**: `badToken`, `badPerms`, `badUnknownVerification`.

### `POST /api/v2/admin/user-verifications/:id/resend`

Resends the verification email for an outstanding row.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `usersWrite{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Path params**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `id` | `string{:ts}` | Yes | Pending verification ID. |

**Response**

`goodAdminUserVerificationResend` (200): `{ id: string }`.

**Errors**: `badToken`, `badPerms`, `badUnknownVerification`.

## Submission audit routes

The submissions table records every flag submission and admin bot job for compliance and abuse investigation. Each row is keyed by submission ID and includes the IP address.

### `GET /api/v2/admin/submissions`

Lists submission audit rows.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `usersWrite{:ts}`, `challsRead{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Query**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `limit` | `number{:ts}` | Yes | Integer from `1` through `100`. |
| `offset` | `number{:ts}` | Yes | Integer ≥ `0`. |
| `sortBy` | `SubmissionSortBy{:ts}` | No | `createdAt`, `challenge`, `team`, `ip`, `kind`, or `result`. |
| `sortOrder` | `SortOrder{:ts}` | No | `asc` or `desc`. |
| `challengeSearch` | `string{:ts}` | No | 1–100 characters. |
| `teamSearch` | `string{:ts}` | No | 1–100 characters. |

**Response**

`goodAdminSubmissions` (200): `{ total: number, submissions: AdminSubmission[] }`. Each row includes submission ID, kind, challenge metadata, user metadata, IP address, result, provider-specific details, optional `relatedId`, and `createdAt`.

**Errors**: `badBody`, `badToken`, `badPerms`.

### `POST /api/v2/admin/submissions`

Lists submission audit rows with rich filters.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `usersWrite{:ts}`, `challsRead{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Query**

Identical to `GET /api/v2/admin/submissions`.

**Request body**

```json
{
  "challenge": { "include": ["challenge-id"] },
  "team": { "include": ["team-id"] },
  "kind": { "include": ["flag", "admin_bot"] },
  "result": { "exclude": ["incorrect"] },
  "teamStatus": { "include": ["not_banned"] },
  "category": { "include": ["web"] },
  "division": { "include": ["open"] },
  "createdAfter": "2026-05-05T09:30:00.000Z",
  "createdBefore": "2026-05-05T10:30:00.000Z"
}
```

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `challenge` | `Filter<string>{:ts}` | No | Challenge IDs. |
| `team` | `Filter<string>{:ts}` | No | Team IDs. |
| `kind` | `Filter<SubmissionKind>{:ts}` | No | `flag`, `admin_bot`. |
| `result` | `Filter<SubmissionResult>{:ts}` | No | `correct`, `incorrect`, `already_solved`, `queued`, `active_job`, `invalid_input`, `bad_instancer_state`. |
| `teamStatus` | `Filter<SubmissionTeamStatus>{:ts}` | No | `banned`, `not_banned`. |
| `category` | `Filter<string>{:ts}` | No | Challenge categories. |
| `division` | `Filter<string>{:ts}` | No | Division keys. |
| `createdAfter` | `string{:ts}` | No | ISO timestamp lower bound. |
| `createdBefore` | `string{:ts}` | No | ISO timestamp upper bound. |

**Response**

Same shape as `GET /api/v2/admin/submissions`.

**Errors**: `badBody`, `badToken`, `badPerms`.

## Settings routes

Runtime settings override values from `rctf.d/*.yaml` config files without restarting the server. Setting a field to `null{:ts}` clears the override.

### `GET /api/v2/admin/settings`

Returns the current overrides and defaults side-by-side.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `settingsWrite{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Response**

`goodAdminSettings` (200): `{ overrides: AdminSettings, defaults: AdminSettings }`. `overrides` are runtime values from the database; `defaults` come from config files and environment variables.

**Errors**: `badToken`, `badPerms`.

### `PUT /api/v2/admin/settings`

Updates runtime overrides. Updating `startTime` or `endTime` forces a leaderboard recalculation.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `settingsWrite{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Request body**

```json
{
  "data": {
    "ctfName": "Example CTF",
    "homeContent": "# Welcome",
    "startTime": 1770000000000,
    "endTime": 1770100000000,
    "sponsors": [
      { "name": "Example", "icon": "https://example.com/icon.png", "description": "Sponsor", "url": "https://example.com" }
    ],
    "meta": { "description": "Example CTF", "imageUrl": "https://example.com/card.png" },
    "faviconUrl": null,
    "logoLightUrl": null,
    "logoDarkUrl": null
  }
}
```

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `data` | `Partial<AdminSettings>{:ts}` | Yes | Nullable values clear overrides. |

**Response**

`goodAdminSettingsUpdate` (200): same shape as the `GET` response (returns the updated overrides and defaults).

**Errors**: `badBody`, `badToken`, `badPerms`.

## Schema routes

### `GET /api/v2/admin/instancer/schema`

Returns the active instancer provider's JSON schema. Used by the challenge editor to validate `instancerConfig{:ts}`.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `challsRead{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Response**

`goodInstancerSchema` (200): `{ schema: Record<string, unknown>, defaults: Record<string, unknown> }`.

**Errors**: `badToken`, `badPerms`, `badEndpoint` (no instancer provider configured).

### `GET /api/v2/admin/admin-bot/status`

Returns admin bot enablement and source language.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `challsRead{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Response**

`goodAdminBotStatus` (200): `{ enabled: boolean, configLanguage: string }`.

**Errors**: `badToken`, `badPerms`.

## Admin bot service routes

These routes are not user-admin endpoints. They use the shared admin bot bearer token (`adminBot.provider.options.secretKey{:yaml}` in `rctf.d/`) and are intended for the admin bot worker only.

### `POST /api/v2/admin/admin-bot/jobs/pull`

Claims the oldest queued job for processing.

| Property | Value |
| --- | --- |
| Auth | Service |
| Gate | None |
| Permissions | — |
| Captcha action | — |
| Rate limit | — |

**Response**

`goodAdminBotJobPull` (200): `{ job: PulledAdminBotJob \| null }`. When no job is queued, returns `{ job: null }`.

A pulled job contains `id`, `challengeId`, `configRevision`, `userId`, `submittedAt`, `flag`, submitted `inputs`, and `instancerInstances`.

**Errors**: `badToken`.

### `GET /api/v2/admin/admin-bot/challenges/:id/source`

Fetches the challenge source code for a specific config revision.

| Property | Value |
| --- | --- |
| Auth | Service |
| Gate | None |
| Permissions | — |
| Captcha action | — |
| Rate limit | — |

**Path params**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `id` | `string{:ts}` | Yes | Challenge ID. |

**Response**

`goodAdminBotChallengeSource` (200): `{ sourceCode: string, configRevision: string }`.

**Errors**: `badToken`, `badChallenge`.

### `POST /api/v2/admin/admin-bot/jobs/:id/complete`

Marks a running job as completed and stores logs.

| Property | Value |
| --- | --- |
| Auth | Service |
| Gate | None |
| Permissions | — |
| Captcha action | — |
| Rate limit | — |

**Path params**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `id` | `string{:ts}` | Yes | Job ID. |

**Request body**

```json
{
  "logs": "newline-delimited log data"
}
```

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `logs` | `string \| undefined{:ts}` | No | Newline-delimited JSON. Maximum `1048576` characters. |

**Response**

`goodAdminBotJobUpdate` (200): `{ ok: boolean }`.

**Errors**: `badBody`, `badToken`.

### `POST /api/v2/admin/admin-bot/jobs/:id/fail`

Marks a running job as failed and stores logs.

| Property | Value |
| --- | --- |
| Auth | Service |
| Gate | None |
| Permissions | — |
| Captcha action | — |
| Rate limit | — |

**Path params**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `id` | `string{:ts}` | Yes | Job ID. |

**Request body**

Same shape as `complete`.

**Response**

`goodAdminBotJobUpdate` (200): `{ ok: boolean }`.

**Errors**: `badBody`, `badToken`.

### `GET /api/v2/admin/admin-bot/queue-depth`

Returns the number of queued jobs.

| Property | Value |
| --- | --- |
| Auth | Service |
| Gate | None |
| Permissions | — |
| Captcha action | — |
| Rate limit | — |

**Response**

`goodAdminBotQueueDepth` (200): `{ depth: number }`.

**Errors**: `badToken`.

## V1 compatibility

### Challenges

#### `GET /api/v1/admin/challs`

Same as [`GET /api/v2/admin/challs`](#get-apiv2adminchalls), but V1 admin challenge data omits `hidden`, `releaseTime`, `instancerConfig`, `adminBotConfig`, and file `size`.

#### `PUT /api/v1/admin/challs/:id`

Same as [`PUT /api/v2/admin/challs/:id`](#put-apiv2adminchallsid), with the V1 challenge data shape (no `hidden`, `releaseTime`, `instancerConfig`, `adminBotConfig`, or file `size`).

#### `DELETE /api/v1/admin/challs/:id`

V1-only. Deletes a challenge and all its solves. Triggers a leaderboard recalculation. No V2 equivalent — V2 deletes via challenge mutation in the admin panel.

| Property | Value |
| --- | --- |
| Auth | Required |
| Gate | None |
| Permissions | `challsWrite{:ts}` |
| Captcha action | — |
| Rate limit | — |

**Path params**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `id` | `string{:ts}` | Yes | Challenge ID. |

**Response**

`goodChallengeDelete` (200, no data).

**Errors**: `badToken`, `badPerms`, `badChallenge`.

### Uploads

#### `POST /api/v1/admin/upload`

Differs from V2: accepts JSON with base64-encoded data URIs instead of `multipart/form-data`.

**Request body**

```json
{
  "files": [
    { "name": "dist.zip", "data": "data:application/zip;base64,..." }
  ]
}
```

V1 upload responses omit the `size` field.

**Errors**: `badBody`, `badToken`, `badPerms`, `badDataUri`, `badFilesUpload`.

#### `POST /api/v1/admin/upload/query`

Same as [`POST /api/v2/admin/upload/query`](#post-apiv2adminuploadquery), but V1 upload query responses include nullable `url` only — no `size`.

## `AdminChallengeV2`

| Field | Type | Notes |
| --- | --- | --- |
| `id` | `string{:ts}` | Challenge ID. |
| `name` | `string{:ts}` | Display name. |
| `description` | `string{:ts}` | Markdown description. |
| `category` | `string{:ts}` | Category name. |
| `author` | `string{:ts}` | Author. |
| `files` | `ChallengeFileV2[]{:ts}` | Files. See [`ChallengeFileV2`](/api/responses#challengefilev2). |
| `points` | `{ min: number, max: number }{:ts}` | Dynamic scoring bounds. |
| `flag` | `string{:ts}` | Correct flag. |
| `tiebreakEligible` | `boolean{:ts}` | Whether solves count toward the tiebreak timestamp. |
| `sortWeight` | `number \| null \| undefined{:ts}` | Optional category ordering weight. |
| `instancerConfig` | `InstancerConfig \| null \| undefined{:ts}` | Per-team instance config. See [`InstancerConfig`](/api/responses#instancerconfig). |
| `adminBotConfig` | `AdminBotConfig \| null \| undefined{:ts}` | Admin bot config. See [`AdminBotConfig`](/api/responses#adminbotconfig). |
| `hidden` | `boolean{:ts}` | Hidden challenges are invisible to non-admin users. |
| `releaseTime` | `number \| null \| undefined{:ts}` | Unix millisecond release timestamp. |
