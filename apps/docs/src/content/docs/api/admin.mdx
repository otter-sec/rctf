---
title: Admin
description: Challenge management, uploads, teams, verifications, submissions, settings, and admin bot service routes.
---

Admin routes require a user auth token with the listed permission bits, except for admin bot service routes, which use the shared admin bot bearer token. Routes are grouped by resource. Permissions, captcha actions, and rate limits referenced in property tables are defined on the [API overview](/api/).

## Permissions

| Permission | Required by |
| --- | --- |
| `challsRead{:ts}` | Reading admin challenge data, reading upload state, reading instancer schema, reading admin bot status, and reading admin user detail with solve history. |
| `challsWrite{:ts}` | Updating challenges and uploading files. |
| `challsSolveWrite{:ts}` | Deleting solves. |
| `usersWrite{:ts}` | Listing teams, filtering teams, mutating teams, generating team tokens, managing pending verifications, and reading submission audit logs. |
| `settingsWrite{:ts}` | Reading and updating runtime settings. |

Routes that list multiple permission bits require all of them.

## Routes

| Method | Path | Version | Auth | Gate |
| --- | --- | --- | --- | --- |
| `[36mGET[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22mchalls{:ansi}` | V2 | Required (`challsRead{:ts}`) | None |
| `[36mGET[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22mchalls[2m/[22m:id{:ansi}` | V2 | Required (`challsRead{:ts}`) | None |
| `[35mPUT[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22mchalls[2m/[22m:id{:ansi}` | V2 | Required (`challsWrite{:ts}`) | None |
| `[31mDELETE[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22mchalls[2m/[22m:challengeId[2m/[22msolves[2m/[22m:userId{:ansi}` | V2 | Required (`challsSolveWrite{:ts}`) | None |
| `[32mPOST[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22mupload{:ansi}` | V2 | Required (`challsWrite{:ts}`) | None |
| `[32mPOST[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22mupload[2m/[22mquery{:ansi}` | V2 | Required (`challsRead{:ts}`) | None |
| `[36mGET[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22musers{:ansi}` | V2 | Required (`usersWrite{:ts}`) | None |
| `[32mPOST[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22musers{:ansi}` | V2 | Required (`usersWrite{:ts}`) | None |
| `[36mGET[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22musers[2m/[22m:id{:ansi}` | V2 | Required (`usersWrite{:ts}`, `challsRead{:ts}`) | None |
| `[35mPUT[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22musers[2m/[22m:id{:ansi}` | V2 | Required (`usersWrite{:ts}`) | None |
| `[31mDELETE[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22musers[2m/[22m:id{:ansi}` | V2 | Required (`usersWrite{:ts}`) | None |
| `[32mPOST[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22musers[2m/[22m:id[2m/[22mtoken{:ansi}` | V2 | Required (`usersWrite{:ts}`) | None |
| `[36mGET[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22muser-verifications{:ansi}` | V2 | Required (`usersWrite{:ts}`) | None |
| `[32mPOST[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22muser-verifications[2m/[22m:id[2m/[22mcomplete{:ansi}` | V2 | Required (`usersWrite{:ts}`) | None |
| `[32mPOST[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22muser-verifications[2m/[22m:id[2m/[22mresend{:ansi}` | V2 | Required (`usersWrite{:ts}`) | None |
| `[36mGET[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22msubmissions{:ansi}` | V2 | Required (`usersWrite{:ts}`, `challsRead{:ts}`) | None |
| `[32mPOST[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22msubmissions{:ansi}` | V2 | Required (`usersWrite{:ts}`, `challsRead{:ts}`) | None |
| `[36mGET[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22msettings{:ansi}` | V2 | Required (`settingsWrite{:ts}`) | None |
| `[35mPUT[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22msettings{:ansi}` | V2 | Required (`settingsWrite{:ts}`) | None |
| `[36mGET[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22minstancer[2m/[22mschema{:ansi}` | V2 | Required (`challsRead{:ts}`) | None |
| `[36mGET[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22madmin-bot[2m/[22mstatus{:ansi}` | V2 | Required (`challsRead{:ts}`) | None |
| `[32mPOST[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22madmin-bot[2m/[22mjobs[2m/[22mpull{:ansi}` | V2 | Service | None |
| `[36mGET[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22madmin-bot[2m/[22mchallenges[2m/[22m:id[2m/[22msource{:ansi}` | V2 | Service | None |
| `[32mPOST[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22madmin-bot[2m/[22mjobs[2m/[22m:id[2m/[22mcomplete{:ansi}` | V2 | Service | None |
| `[32mPOST[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22madmin-bot[2m/[22mjobs[2m/[22m:id[2m/[22mfail{:ansi}` | V2 | Service | None |
| `[36mGET[0m{:ansi}` | `[2m/api/[22mv2[2m/[22madmin[2m/[22madmin-bot[2m/[22mqueue-depth{:ansi}` | V2 | Service | None |
| `[36mGET[0m{:ansi}` | `[2m/api/[22mv1[2m/[22madmin[2m/[22mchalls{:ansi}` | V1 | Required (`challsRead{:ts}`) | None |
| `[35mPUT[0m{:ansi}` | `[2m/api/[22mv1[2m/[22madmin[2m/[22mchalls[2m/[22m:id{:ansi}` | V1 | Required (`challsWrite{:ts}`) | None |
| `[31mDELETE[0m{:ansi}` | `[2m/api/[22mv1[2m/[22madmin[2m/[22mchalls[2m/[22m:id{:ansi}` | V1 | Required (`challsWrite{:ts}`) | None |
| `[32mPOST[0m{:ansi}` | `[2m/api/[22mv1[2m/[22madmin[2m/[22mupload{:ansi}` | V1 | Required (`challsWrite{:ts}`) | None |
| `[32mPOST[0m{:ansi}` | `[2m/api/[22mv1[2m/[22madmin[2m/[22mupload[2m/[22mquery{:ansi}` | V1 | Required (`challsRead{:ts}`) | None |

## Challenge routes

### `[36mGET[0m [2m/api/[22mv2[2m/[22madmin[2m/[22mchalls{:ansi}`

Lists every challenge with full admin fields, including hidden challenges and unreleased challenges.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `challsRead{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Response**

`[2;32m200 [22mgoodAdminChallenges[0m{:ansi}`: array of `AdminChallengeV2`. See [`AdminChallengeV2`](#adminchallengev2) below.

**Errors**: `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`.

### `[36mGET[0m [2m/api/[22mv2[2m/[22madmin[2m/[22mchalls[2m/[22m:id{:ansi}`

Returns one admin challenge.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `challsRead{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Path params**

| Field | Type          | Required | Notes         |
| ----- | ------------- | -------- | ------------- |
| `id`  | `string{:ts}` | Yes      | Challenge ID. |

**Response**

`[2;32m200 [22mgoodAdminChallenge[0m{:ansi}`: `AdminChallengeV2` object. See [`AdminChallengeV2`](#adminchallengev2).

**Errors**: `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`, `[2;31m404 [22mbadChallenge[0m{:ansi}`.

### `[35mPUT[0m [2m/api/[22mv2[2m/[22madmin[2m/[22mchalls[2m/[22m:id{:ansi}`

Creates or updates a challenge. Omitted fields in `data` keep their current value. Setting `instancerConfig{:ts}` or `adminBotConfig{:ts}` to `null{:ts}` clears the integration config.

| Property       | Value              |
| -------------- | ------------------ |
| Auth           | Required           |
| Gate           | None               |
| Permissions    | `challsWrite{:ts}` |
| Captcha action | —                  |
| Rate limit     | —                  |

**Path params**

| Field | Type          | Required | Notes                                 |
| ----- | ------------- | -------- | ------------------------------------- |
| `id`  | `string{:ts}` | Yes      | Challenge ID. Created if not present. |

**Request body**

```json
{
  "data": {
    "name": "web demo",
    "category": "web",
    "author": "organizers",
    "description": "Solve it.",
    "flag": "flag{example}",
    "points": { "min": 100, "max": 500 },
    "tiebreakEligible": true,
    "files": [{ "name": "dist.zip", "url": "/uploads/dist.zip", "size": 12345 }],
    "sortWeight": 10,
    "instancerConfig": null,
    "adminBotConfig": null,
    "hidden": false,
    "releaseTime": null
  }
}
```

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `data` | `Partial<AdminChallengeV2>{:ts}` | Yes | Partial challenge data. Omitted fields keep existing values. |

`instancerConfig{:ts}` is validated against the active instancer provider schema. `adminBotConfig{:ts}` source is validated by the active admin bot provider and stored with a generated revision. Updating a challenge triggers a leaderboard recalculation.

**Response**

`[2;32m200 [22mgoodChallengeUpdate[0m{:ansi}`: updated `AdminChallengeV2`.

**Errors**: `[2;31m400 [22mbadBody[0m{:ansi}`, `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`, `[2;31m400 [22mbadInstancerConfig[0m{:ansi}`, `[2;31m400 [22mbadAdminBotConfig[0m{:ansi}`.

### `[31mDELETE[0m [2m/api/[22mv2[2m/[22madmin[2m/[22mchalls[2m/[22m:challengeId[2m/[22msolves[2m/[22m:userId{:ansi}`

Deletes one solve for a specific user and challenge. Triggers a leaderboard recalculation.

| Property       | Value                   |
| -------------- | ----------------------- |
| Auth           | Required                |
| Gate           | None                    |
| Permissions    | `challsSolveWrite{:ts}` |
| Captcha action | —                       |
| Rate limit     | —                       |

**Path params**

| Field         | Type          | Required | Notes                                 |
| ------------- | ------------- | -------- | ------------------------------------- |
| `challengeId` | `string{:ts}` | Yes      | Challenge ID.                         |
| `userId`      | `string{:ts}` | Yes      | Team ID whose solve is being removed. |

**Response**

`[2;32m200 [22mgoodChallengeSolveDelete[0m{:ansi}` (no data).

**Errors**: `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`, `[2;31m404 [22mbadUnknownSolve[0m{:ansi}`.

## Upload routes

### `[32mPOST[0m [2m/api/[22mv2[2m/[22madmin[2m/[22mupload{:ansi}`

Uploads one or more files. Files are deduplicated by SHA-256 — uploading the same content twice returns the existing URL.

| Property       | Value              |
| -------------- | ------------------ |
| Auth           | Required           |
| Gate           | None               |
| Permissions    | `challsWrite{:ts}` |
| Captcha action | —                  |
| Rate limit     | —                  |

**Request**

`multipart/form-data` with one or more `files` form fields.

**Response**

`[2;32m200 [22mgoodFilesUpload[0m{:ansi}`: array of upload objects.

| Field  | Type                  | Notes                |
| ------ | --------------------- | -------------------- |
| `name` | `string{:ts}`         | Display filename.    |
| `url`  | `string{:ts}`         | Public download URL. |
| `size` | `number \| null{:ts}` | File size in bytes.  |

**Errors**: `[2;31m400 [22mbadBody[0m{:ansi}`, `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`, `[2;31m500 [22mbadFilesUpload[0m{:ansi}`.

### `[32mPOST[0m [2m/api/[22mv2[2m/[22madmin[2m/[22mupload[2m/[22mquery{:ansi}`

Checks whether files already exist by SHA-256 without uploading them.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `challsRead{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Request body**

```json
{
  "uploads": [{ "sha256": "<hex-digest>", "name": "dist.zip" }]
}
```

| Field              | Type          | Required | Notes                       |
| ------------------ | ------------- | -------- | --------------------------- |
| `uploads[].sha256` | `string{:ts}` | Yes      | Hex-encoded SHA-256 digest. |
| `uploads[].name`   | `string{:ts}` | Yes      | Display filename.           |

**Response**

`[2;32m200 [22mgoodUploadsQuery[0m{:ansi}`: array with one entry per query — each entry has nullable `url` and nullable `size`. A `null{:ts}` URL means the file is not yet uploaded.

**Errors**: `[2;31m400 [22mbadBody[0m{:ansi}`, `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`.

## Team routes

### `[36mGET[0m [2m/api/[22mv2[2m/[22madmin[2m/[22musers{:ansi}`

Lists teams. Query parameters control pagination, sorting, and search.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `usersWrite{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Query**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `limit` | `number{:ts}` | Yes | Integer from `1` through `100`. |
| `offset` | `number{:ts}` | Yes | Integer ≥ `0`. |
| `search` | `string{:ts}` | No | 1–100 characters. |
| `sortBy` | `AdminTeamSortBy{:ts}` | No | `createdAt`, `team`, `email`, `division`, `score`, `solves`, or `status`. |
| `sortOrder` | `SortOrder{:ts}` | No | `asc` or `desc`. |

**Response**

`[2;32m200 [22mgoodAdminUsers[0m{:ansi}`: `{ total: number, users: AdminUserListItem[] }`. Each list item includes ID, name, email, division, perms, banned state, score, solve count, avatar URL, country code, status text, and `createdAt`.

**Errors**: `[2;31m400 [22mbadBody[0m{:ansi}`, `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`.

### `[32mPOST[0m [2m/api/[22mv2[2m/[22madmin[2m/[22musers{:ansi}`

Lists teams with rich filters. Same query parameters as `[36mGET[0m{:ansi}`, with filter object in the body.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `usersWrite{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Query**

Identical to `[36mGET[0m [2m/api/[22mv2[2m/[22madmin[2m/[22musers{:ansi}`.

**Request body**

```json
{
  "status": { "include": ["active"], "exclude": ["banned"] },
  "division": { "include": ["open"] }
}
```

| Field      | Type                           | Required | Notes                        |
| ---------- | ------------------------------ | -------- | ---------------------------- |
| `status`   | `Filter<AdminTeamStatus>{:ts}` | No       | `active`, `banned`, `admin`. |
| `division` | `Filter<string>{:ts}`          | No       | Division keys.               |

Filter objects accept nullable `include` and `exclude` arrays.

**Response**

Same shape as `[36mGET[0m [2m/api/[22mv2[2m/[22madmin[2m/[22musers{:ansi}`.

**Errors**: `[2;31m400 [22mbadBody[0m{:ansi}`, `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`.

### `[36mGET[0m [2m/api/[22mv2[2m/[22madmin[2m/[22musers[2m/[22m:id{:ansi}`

Returns full team details, including solve history.

| Property       | Value                                |
| -------------- | ------------------------------------ |
| Auth           | Required                             |
| Gate           | None                                 |
| Permissions    | `usersWrite{:ts}`, `challsRead{:ts}` |
| Captcha action | —                                    |
| Rate limit     | —                                    |

**Path params**

| Field | Type          | Required | Notes    |
| ----- | ------------- | -------- | -------- |
| `id`  | `string{:ts}` | Yes      | Team ID. |

**Response**

`[2;32m200 [22mgoodAdminUser[0m{:ansi}`:

| Field | Type | Notes |
| --- | --- | --- |
| `id` | `string{:ts}` | Team ID. |
| `name` | `string{:ts}` | Team name. |
| `email` | `string \| null{:ts}` | Team email. |
| `division` | `string{:ts}` | Division key. |
| `perms` | `number{:ts}` | Permission bitmask. |
| `banned` | `boolean{:ts}` | Whether the team is banned. |
| `score` | `number{:ts}` | Cached current score. |
| `solveCount` | `number{:ts}` | Cached solve count. |
| `avatarUrl` | `string \| null{:ts}` | Avatar URL. |
| `countryCode` | `string \| null{:ts}` | Country code. |
| `statusText` | `string \| null{:ts}` | Status text. |
| `createdAt` | `string{:ts}` | ISO timestamp. |
| `solves[]` | `{ challengeId, challengeName, challengeCategory, createdAt }[]{:ts}` | Solve history. |

**Errors**: `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`, `[2;31m404 [22mbadUnknownUser[0m{:ansi}`.

### `[35mPUT[0m [2m/api/[22mv2[2m/[22madmin[2m/[22musers[2m/[22m:id{:ansi}`

Mutates team state. Admins cannot mutate users with admin permissions.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `usersWrite{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Path params**

| Field | Type          | Required | Notes    |
| ----- | ------------- | -------- | -------- |
| `id`  | `string{:ts}` | Yes      | Team ID. |

**Request body**

```json
{
  "data": { "banned": true }
}
```

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `data.banned` | `boolean{:ts}` | No | Banning removes the team from leaderboard output after recalculation without deleting solves. Unbanning restores rank after recalculation. |

**Response**

`[2;32m200 [22mgoodAdminUserUpdate[0m{:ansi}` (no data).

**Errors**: `[2;31m400 [22mbadBody[0m{:ansi}`, `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`, `[2;31m404 [22mbadUnknownUser[0m{:ansi}`, `[2;31m403 [22mbadUserPrivileged[0m{:ansi}`.

### `[31mDELETE[0m [2m/api/[22mv2[2m/[22madmin[2m/[22musers[2m/[22m:id{:ansi}`

Deletes a team. Cannot delete users with admin permissions.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `usersWrite{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Path params**

| Field | Type          | Required | Notes    |
| ----- | ------------- | -------- | -------- |
| `id`  | `string{:ts}` | Yes      | Team ID. |

**Response**

`[2;32m200 [22mgoodAdminUserDelete[0m{:ansi}` (no data).

**Errors**: `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`, `[2;31m404 [22mbadUnknownUser[0m{:ansi}`, `[2;31m403 [22mbadUserPrivileged[0m{:ansi}`.

### `[32mPOST[0m [2m/api/[22mv2[2m/[22madmin[2m/[22musers[2m/[22m:id[2m/[22mtoken{:ansi}`

Generates a fresh long-lived team token. Useful for recovering teams that have lost access.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `usersWrite{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Path params**

| Field | Type          | Required | Notes    |
| ----- | ------------- | -------- | -------- |
| `id`  | `string{:ts}` | Yes      | Team ID. |

**Response**

`[2;32m200 [22mgoodCreateUserToken[0m{:ansi}`: `{ token: string }`.

**Errors**: `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`, `[2;31m404 [22mbadUnknownUser[0m{:ansi}`, `[2;31m403 [22mbadUserPrivileged[0m{:ansi}`.

## Pending verification routes

Pending verification rows represent users who registered with an email but have not yet completed email verification.

### `[36mGET[0m [2m/api/[22mv2[2m/[22madmin[2m/[22muser-verifications{:ansi}`

Lists outstanding pending verifications.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `usersWrite{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Response**

`[2;32m200 [22mgoodAdminUserVerifications[0m{:ansi}`: `{ verifications: PendingUserVerification[] }`. Each row contains `id`, `name`, `email`, `division`, `createdAt`, and `expiresAt`.

**Errors**: `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`.

### `[32mPOST[0m [2m/api/[22mv2[2m/[22madmin[2m/[22muser-verifications[2m/[22m:id[2m/[22mcomplete{:ansi}`

Manually completes a pending verification, creating the team without requiring the user to follow the verification email link.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `usersWrite{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Path params**

| Field | Type          | Required | Notes                    |
| ----- | ------------- | -------- | ------------------------ |
| `id`  | `string{:ts}` | Yes      | Pending verification ID. |

**Response**

`[2;32m200 [22mgoodAdminUserVerificationComplete[0m{:ansi}`: `{ userId: string }`.

**Errors**: `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`, `[2;31m404 [22mbadUnknownVerification[0m{:ansi}`.

### `[32mPOST[0m [2m/api/[22mv2[2m/[22madmin[2m/[22muser-verifications[2m/[22m:id[2m/[22mresend{:ansi}`

Resends the verification email for an outstanding row.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `usersWrite{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Path params**

| Field | Type          | Required | Notes                    |
| ----- | ------------- | -------- | ------------------------ |
| `id`  | `string{:ts}` | Yes      | Pending verification ID. |

**Response**

`[2;32m200 [22mgoodAdminUserVerificationResend[0m{:ansi}`: `{ id: string }`.

**Errors**: `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`, `[2;31m404 [22mbadUnknownVerification[0m{:ansi}`.

## Submission audit routes

The submissions table records every flag submission and admin bot job for compliance and abuse investigation. Each row is keyed by submission ID and includes the IP address.

### `[36mGET[0m [2m/api/[22mv2[2m/[22madmin[2m/[22msubmissions{:ansi}`

Lists submission audit rows.

| Property       | Value                                |
| -------------- | ------------------------------------ |
| Auth           | Required                             |
| Gate           | None                                 |
| Permissions    | `usersWrite{:ts}`, `challsRead{:ts}` |
| Captcha action | —                                    |
| Rate limit     | —                                    |

**Query**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `limit` | `number{:ts}` | Yes | Integer from `1` through `100`. |
| `offset` | `number{:ts}` | Yes | Integer ≥ `0`. |
| `sortBy` | `SubmissionSortBy{:ts}` | No | `createdAt`, `challenge`, `team`, `ip`, `kind`, or `result`. |
| `sortOrder` | `SortOrder{:ts}` | No | `asc` or `desc`. |
| `challengeSearch` | `string{:ts}` | No | 1–100 characters. |
| `teamSearch` | `string{:ts}` | No | 1–100 characters. |

**Response**

`[2;32m200 [22mgoodAdminSubmissions[0m{:ansi}`: `{ total: number, submissions: AdminSubmission[] }`. Each row includes submission ID, kind, challenge metadata, user metadata, IP address, result, provider-specific details, optional `relatedId`, and `createdAt`.

**Errors**: `[2;31m400 [22mbadBody[0m{:ansi}`, `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`.

### `[32mPOST[0m [2m/api/[22mv2[2m/[22madmin[2m/[22msubmissions{:ansi}`

Lists submission audit rows with rich filters.

| Property       | Value                                |
| -------------- | ------------------------------------ |
| Auth           | Required                             |
| Gate           | None                                 |
| Permissions    | `usersWrite{:ts}`, `challsRead{:ts}` |
| Captcha action | —                                    |
| Rate limit     | —                                    |

**Query**

Identical to `[36mGET[0m [2m/api/[22mv2[2m/[22madmin[2m/[22msubmissions{:ansi}`.

**Request body**

```json
{
  "challenge": { "include": ["challenge-id"] },
  "team": { "include": ["team-id"] },
  "kind": { "include": ["flag", "admin_bot"] },
  "result": { "exclude": ["incorrect"] },
  "teamStatus": { "include": ["not_banned"] },
  "category": { "include": ["web"] },
  "division": { "include": ["open"] },
  "createdAfter": "2026-05-05T09:30:00.000Z",
  "createdBefore": "2026-05-05T10:30:00.000Z"
}
```

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `challenge` | `Filter<string>{:ts}` | No | Challenge IDs. |
| `team` | `Filter<string>{:ts}` | No | Team IDs. |
| `kind` | `Filter<SubmissionKind>{:ts}` | No | `flag`, `admin_bot`. |
| `result` | `Filter<SubmissionResult>{:ts}` | No | `correct`, `incorrect`, `already_solved`, `queued`, `active_job`, `invalid_input`, `bad_instancer_state`. |
| `teamStatus` | `Filter<SubmissionTeamStatus>{:ts}` | No | `banned`, `not_banned`. |
| `category` | `Filter<string>{:ts}` | No | Challenge categories. |
| `division` | `Filter<string>{:ts}` | No | Division keys. |
| `createdAfter` | `string{:ts}` | No | ISO timestamp lower bound. |
| `createdBefore` | `string{:ts}` | No | ISO timestamp upper bound. |

**Response**

Same shape as `[36mGET[0m [2m/api/[22mv2[2m/[22madmin[2m/[22msubmissions{:ansi}`.

**Errors**: `[2;31m400 [22mbadBody[0m{:ansi}`, `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`.

## Settings routes

Runtime settings override values from `rctf.d/*.yaml` config files without restarting the server. Setting a field to `null{:ts}` clears the override.

### `[36mGET[0m [2m/api/[22mv2[2m/[22madmin[2m/[22msettings{:ansi}`

Returns the current overrides and defaults side-by-side.

| Property       | Value                |
| -------------- | -------------------- |
| Auth           | Required             |
| Gate           | None                 |
| Permissions    | `settingsWrite{:ts}` |
| Captcha action | —                    |
| Rate limit     | —                    |

**Response**

`[2;32m200 [22mgoodAdminSettings[0m{:ansi}`: `{ overrides: AdminSettings, defaults: AdminSettings }`. `overrides` are runtime values from the database; `defaults` come from config files and environment variables.

**Errors**: `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`.

### `[35mPUT[0m [2m/api/[22mv2[2m/[22madmin[2m/[22msettings{:ansi}`

Updates runtime overrides. Updating `startTime` or `endTime` forces a leaderboard recalculation.

| Property       | Value                |
| -------------- | -------------------- |
| Auth           | Required             |
| Gate           | None                 |
| Permissions    | `settingsWrite{:ts}` |
| Captcha action | —                    |
| Rate limit     | —                    |

**Request body**

```json
{
  "data": {
    "ctfName": "Example CTF",
    "homeContent": "# Welcome",
    "startTime": 1770000000000,
    "endTime": 1770100000000,
    "sponsors": [
      {
        "name": "Example",
        "icon": "https://example.com/icon.png",
        "description": "Sponsor",
        "url": "https://example.com"
      }
    ],
    "meta": { "description": "Example CTF", "imageUrl": "https://example.com/card.png" },
    "faviconUrl": null,
    "logoLightUrl": null,
    "logoDarkUrl": null
  }
}
```

| Field  | Type                          | Required | Notes                            |
| ------ | ----------------------------- | -------- | -------------------------------- |
| `data` | `Partial<AdminSettings>{:ts}` | Yes      | Nullable values clear overrides. |

**Response**

`[2;32m200 [22mgoodAdminSettingsUpdate[0m{:ansi}`: same shape as the `[36mGET[0m{:ansi}` response (returns the updated overrides and defaults).

**Errors**: `[2;31m400 [22mbadBody[0m{:ansi}`, `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`.

## Schema routes

### `[36mGET[0m [2m/api/[22mv2[2m/[22madmin[2m/[22minstancer[2m/[22mschema{:ansi}`

Returns the active instancer provider's JSON schema. Used by the challenge editor to validate `instancerConfig{:ts}`.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `challsRead{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Response**

`[2;32m200 [22mgoodInstancerSchema[0m{:ansi}`: `{ schema: Record<string, unknown>, defaults: Record<string, unknown> }`.

**Errors**: `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`, `[2;31m404 [22mbadEndpoint[0m{:ansi}` (no instancer provider configured).

### `[36mGET[0m [2m/api/[22mv2[2m/[22madmin[2m/[22madmin-bot[2m/[22mstatus{:ansi}`

Returns admin bot enablement and source language.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `challsRead{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Response**

`[2;32m200 [22mgoodAdminBotStatus[0m{:ansi}`: `{ enabled: boolean, configLanguage: string }`.

**Errors**: `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`.

## Admin bot service routes

These routes are not user-admin endpoints. They use the shared admin bot bearer token (`adminBot.provider.options.secretKey{:yaml}` in `rctf.d/`) and are intended for the admin bot worker only.

### `[32mPOST[0m [2m/api/[22mv2[2m/[22madmin[2m/[22madmin-bot[2m/[22mjobs[2m/[22mpull{:ansi}`

Claims the oldest queued job for processing.

| Property       | Value   |
| -------------- | ------- |
| Auth           | Service |
| Gate           | None    |
| Permissions    | —       |
| Captcha action | —       |
| Rate limit     | —       |

**Response**

`[2;32m200 [22mgoodAdminBotJobPull[0m{:ansi}`: `{ job: PulledAdminBotJob \| null }`. When no job is queued, returns `{ job: null }`.

A pulled job contains `id`, `challengeId`, `configRevision`, `userId`, `submittedAt`, `flag`, submitted `inputs`, and `instancerInstances`.

**Errors**: `[2;31m401 [22mbadToken[0m{:ansi}`.

### `[36mGET[0m [2m/api/[22mv2[2m/[22madmin[2m/[22madmin-bot[2m/[22mchallenges[2m/[22m:id[2m/[22msource{:ansi}`

Fetches the challenge source code for a specific config revision.

| Property       | Value   |
| -------------- | ------- |
| Auth           | Service |
| Gate           | None    |
| Permissions    | —       |
| Captcha action | —       |
| Rate limit     | —       |

**Path params**

| Field | Type          | Required | Notes         |
| ----- | ------------- | -------- | ------------- |
| `id`  | `string{:ts}` | Yes      | Challenge ID. |

**Response**

`[2;32m200 [22mgoodAdminBotChallengeSource[0m{:ansi}`: `{ sourceCode: string, configRevision: string }`.

**Errors**: `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m404 [22mbadChallenge[0m{:ansi}`.

### `[32mPOST[0m [2m/api/[22mv2[2m/[22madmin[2m/[22madmin-bot[2m/[22mjobs[2m/[22m:id[2m/[22mcomplete{:ansi}`

Marks a running job as completed and stores logs.

| Property       | Value   |
| -------------- | ------- |
| Auth           | Service |
| Gate           | None    |
| Permissions    | —       |
| Captcha action | —       |
| Rate limit     | —       |

**Path params**

| Field | Type          | Required | Notes   |
| ----- | ------------- | -------- | ------- |
| `id`  | `string{:ts}` | Yes      | Job ID. |

**Request body**

```json
{
  "logs": "newline-delimited log data"
}
```

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `logs` | `string \| undefined{:ts}` | No | Newline-delimited JSON. Maximum `1048576` characters. |

**Response**

`[2;32m200 [22mgoodAdminBotJobUpdate[0m{:ansi}`: `{ ok: boolean }`.

**Errors**: `[2;31m400 [22mbadBody[0m{:ansi}`, `[2;31m401 [22mbadToken[0m{:ansi}`.

### `[32mPOST[0m [2m/api/[22mv2[2m/[22madmin[2m/[22madmin-bot[2m/[22mjobs[2m/[22m:id[2m/[22mfail{:ansi}`

Marks a running job as failed and stores logs.

| Property       | Value   |
| -------------- | ------- |
| Auth           | Service |
| Gate           | None    |
| Permissions    | —       |
| Captcha action | —       |
| Rate limit     | —       |

**Path params**

| Field | Type          | Required | Notes   |
| ----- | ------------- | -------- | ------- |
| `id`  | `string{:ts}` | Yes      | Job ID. |

**Request body**

Same shape as `complete`.

**Response**

`[2;32m200 [22mgoodAdminBotJobUpdate[0m{:ansi}`: `{ ok: boolean }`.

**Errors**: `[2;31m400 [22mbadBody[0m{:ansi}`, `[2;31m401 [22mbadToken[0m{:ansi}`.

### `[36mGET[0m [2m/api/[22mv2[2m/[22madmin[2m/[22madmin-bot[2m/[22mqueue-depth{:ansi}`

Returns the number of queued jobs.

| Property       | Value   |
| -------------- | ------- |
| Auth           | Service |
| Gate           | None    |
| Permissions    | —       |
| Captcha action | —       |
| Rate limit     | —       |

**Response**

`[2;32m200 [22mgoodAdminBotQueueDepth[0m{:ansi}`: `{ depth: number }`.

**Errors**: `[2;31m401 [22mbadToken[0m{:ansi}`.

## V1 compatibility

### Challenges

#### `[36mGET[0m [2m/api/[22mv1[2m/[22madmin[2m/[22mchalls{:ansi}`

Same as [`[36mGET[0m [2m/api/[22mv2[2m/[22madmin[2m/[22mchalls{:ansi}`](#get-apiv2adminchalls), but V1 admin challenge data omits `hidden`, `releaseTime`, `instancerConfig`, `adminBotConfig`, and file `size`.

#### `[35mPUT[0m [2m/api/[22mv1[2m/[22madmin[2m/[22mchalls[2m/[22m:id{:ansi}`

Same as [`[35mPUT[0m [2m/api/[22mv2[2m/[22madmin[2m/[22mchalls[2m/[22m:id{:ansi}`](#put-apiv2adminchallsid), with the V1 challenge data shape (no `hidden`, `releaseTime`, `instancerConfig`, `adminBotConfig`, or file `size`).

#### `[31mDELETE[0m [2m/api/[22mv1[2m/[22madmin[2m/[22mchalls[2m/[22m:id{:ansi}`

V1-only. Deletes a challenge and all its solves. Triggers a leaderboard recalculation. No V2 equivalent — V2 deletes via challenge mutation in the admin panel.

| Property       | Value              |
| -------------- | ------------------ |
| Auth           | Required           |
| Gate           | None               |
| Permissions    | `challsWrite{:ts}` |
| Captcha action | —                  |
| Rate limit     | —                  |

**Path params**

| Field | Type          | Required | Notes         |
| ----- | ------------- | -------- | ------------- |
| `id`  | `string{:ts}` | Yes      | Challenge ID. |

**Response**

`[2;32m200 [22mgoodChallengeDelete[0m{:ansi}` (no data).

**Errors**: `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`, `[2;31m404 [22mbadChallenge[0m{:ansi}`.

### Uploads

#### `[32mPOST[0m [2m/api/[22mv1[2m/[22madmin[2m/[22mupload{:ansi}`

Differs from V2: accepts JSON with base64-encoded data URIs instead of `multipart/form-data`.

**Request body**

```json
{
  "files": [{ "name": "dist.zip", "data": "data:application/zip;base64,..." }]
}
```

V1 upload responses omit the `size` field.

**Errors**: `[2;31m400 [22mbadBody[0m{:ansi}`, `[2;31m401 [22mbadToken[0m{:ansi}`, `[2;31m403 [22mbadPerms[0m{:ansi}`, `[2;31m400 [22mbadDataUri[0m{:ansi}`, `[2;31m500 [22mbadFilesUpload[0m{:ansi}`.

#### `[32mPOST[0m [2m/api/[22mv1[2m/[22madmin[2m/[22mupload[2m/[22mquery{:ansi}`

Same as [`[32mPOST[0m [2m/api/[22mv2[2m/[22madmin[2m/[22mupload[2m/[22mquery{:ansi}`](#post-apiv2adminuploadquery), but V1 upload query responses include nullable `url` only — no `size`.

## `AdminChallengeV2`

| Field | Type | Notes |
| --- | --- | --- |
| `id` | `string{:ts}` | Challenge ID. |
| `name` | `string{:ts}` | Display name. |
| `description` | `string{:ts}` | Markdown description. |
| `category` | `string{:ts}` | Category name. |
| `author` | `string{:ts}` | Author. |
| `files` | `ChallengeFileV2[]{:ts}` | Files. See [`ChallengeFileV2`](/api/responses/#challengefilev2). |
| `points` | `{ min: number, max: number }{:ts}` | Dynamic scoring bounds. |
| `flag` | `string{:ts}` | Correct flag. |
| `tiebreakEligible` | `boolean{:ts}` | Whether solves count toward the tiebreak timestamp. |
| `sortWeight` | `number \| null \| undefined{:ts}` | Optional category ordering weight. |
| `instancerConfig` | `InstancerConfig \| null \| undefined{:ts}` | Per-team instance config. See [`InstancerConfig`](/api/responses/#instancerconfig). |
| `adminBotConfig` | `AdminBotConfig \| null \| undefined{:ts}` | Admin bot config. See [`AdminBotConfig`](/api/responses/#adminbotconfig). |
| `hidden` | `boolean{:ts}` | Hidden challenges are invisible to non-admin users. |
| `releaseTime` | `number \| null \| undefined{:ts}` | Unix millisecond release timestamp. |
