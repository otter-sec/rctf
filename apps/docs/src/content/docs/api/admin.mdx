---
title: Admin
description: Challenge management, uploads, teams, verifications, submissions, settings, and admin bot service routes.
---

Admin routes require a user auth token with the listed permission bits, except for admin bot service routes, which use the shared admin bot bearer token. Routes are grouped by resource. Permissions, captcha actions, and rate limits referenced in property tables are defined on the [API overview](/api/).

## Permissions

| Permission | Required by |
| --- | --- |
| `challsRead{:ts}` | Reading admin challenge data, reading upload state, reading instancer schema, reading admin bot status, and reading admin user detail with solve history. |
| `challsWrite{:ts}` | Updating challenges and uploading files. |
| `challsSolveWrite{:ts}` | Deleting solves. |
| `usersWrite{:ts}` | Listing teams, filtering teams, mutating teams, generating team tokens, managing pending verifications, and reading submission audit logs. |
| `settingsWrite{:ts}` | Reading and updating runtime settings. |

Routes that list multiple permission bits require all of them.

## Routes

| Method | Path | Version | Auth | Gate |
| --- | --- | --- | --- | --- |
| `<cyan>GET</cyan>` | `<black>/api/v2/admin/challs</black>` | V2 | Required (`challsRead{:ts}`) | None |
| `<cyan>GET</cyan>` | `<black>/api/v2/admin/challs/:id</black>` | V2 | Required (`challsRead{:ts}`) | None |
| `<magenta>PUT</magenta>` | `<black>/api/v2/admin/challs/:id</black>` | V2 | Required (`challsWrite{:ts}`) | None |
| `<red>DELETE</red>` | `<black>/api/v2/admin/challs/:challengeId/solves/:userId</black>` | V2 | Required (`challsSolveWrite{:ts}`) | None |
| `<green>POST</green>` | `<black>/api/v2/admin/upload</black>` | V2 | Required (`challsWrite{:ts}`) | None |
| `<green>POST</green>` | `<black>/api/v2/admin/upload/query</black>` | V2 | Required (`challsRead{:ts}`) | None |
| `<cyan>GET</cyan>` | `<black>/api/v2/admin/users</black>` | V2 | Required (`usersWrite{:ts}`) | None |
| `<green>POST</green>` | `<black>/api/v2/admin/users</black>` | V2 | Required (`usersWrite{:ts}`) | None |
| `<cyan>GET</cyan>` | `<black>/api/v2/admin/users/:id</black>` | V2 | Required (`usersWrite{:ts}`, `challsRead{:ts}`) | None |
| `<magenta>PUT</magenta>` | `<black>/api/v2/admin/users/:id</black>` | V2 | Required (`usersWrite{:ts}`) | None |
| `<red>DELETE</red>` | `<black>/api/v2/admin/users/:id</black>` | V2 | Required (`usersWrite{:ts}`) | None |
| `<green>POST</green>` | `<black>/api/v2/admin/users/:id/token</black>` | V2 | Required (`usersWrite{:ts}`) | None |
| `<cyan>GET</cyan>` | `<black>/api/v2/admin/user-verifications</black>` | V2 | Required (`usersWrite{:ts}`) | None |
| `<green>POST</green>` | `<black>/api/v2/admin/user-verifications/:id/complete</black>` | V2 | Required (`usersWrite{:ts}`) | None |
| `<green>POST</green>` | `<black>/api/v2/admin/user-verifications/:id/resend</black>` | V2 | Required (`usersWrite{:ts}`) | None |
| `<cyan>GET</cyan>` | `<black>/api/v2/admin/submissions</black>` | V2 | Required (`usersWrite{:ts}`, `challsRead{:ts}`) | None |
| `<green>POST</green>` | `<black>/api/v2/admin/submissions</black>` | V2 | Required (`usersWrite{:ts}`, `challsRead{:ts}`) | None |
| `<cyan>GET</cyan>` | `<black>/api/v2/admin/settings</black>` | V2 | Required (`settingsWrite{:ts}`) | None |
| `<magenta>PUT</magenta>` | `<black>/api/v2/admin/settings</black>` | V2 | Required (`settingsWrite{:ts}`) | None |
| `<cyan>GET</cyan>` | `<black>/api/v2/admin/instancer/schema</black>` | V2 | Required (`challsRead{:ts}`) | None |
| `<cyan>GET</cyan>` | `<black>/api/v2/admin/admin-bot/status</black>` | V2 | Required (`challsRead{:ts}`) | None |
| `<green>POST</green>` | `<black>/api/v2/admin/admin-bot/jobs/pull</black>` | V2 | Service | None |
| `<cyan>GET</cyan>` | `<black>/api/v2/admin/admin-bot/challenges/:id/source</black>` | V2 | Service | None |
| `<green>POST</green>` | `<black>/api/v2/admin/admin-bot/jobs/:id/complete</black>` | V2 | Service | None |
| `<green>POST</green>` | `<black>/api/v2/admin/admin-bot/jobs/:id/fail</black>` | V2 | Service | None |
| `<cyan>GET</cyan>` | `<black>/api/v2/admin/admin-bot/queue-depth</black>` | V2 | Service | None |
| `<cyan>GET</cyan>` | `<black>/api/v1/admin/challs</black>` | V1 | Required (`challsRead{:ts}`) | None |
| `<magenta>PUT</magenta>` | `<black>/api/v1/admin/challs/:id</black>` | V1 | Required (`challsWrite{:ts}`) | None |
| `<red>DELETE</red>` | `<black>/api/v1/admin/challs/:id</black>` | V1 | Required (`challsWrite{:ts}`) | None |
| `<green>POST</green>` | `<black>/api/v1/admin/upload</black>` | V1 | Required (`challsWrite{:ts}`) | None |
| `<green>POST</green>` | `<black>/api/v1/admin/upload/query</black>` | V1 | Required (`challsRead{:ts}`) | None |

## Challenge routes

### `<cyan>GET</cyan> <black>/api/v2/admin/challs</black>`

Lists every challenge with full admin fields, including hidden challenges and unreleased challenges.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `challsRead{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Response**

`<green>200 goodAdminChallenges</green>`: array of `AdminChallengeV2`. See [`AdminChallengeV2`](#adminchallengev2) below.

**Errors**: `<red>401 badToken</red>`, `<red>403 badPerms</red>`.

### `<cyan>GET</cyan> <black>/api/v2/admin/challs/:id</black>`

Returns one admin challenge.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `challsRead{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Path params**

| Field | Type          | Required | Notes         |
| ----- | ------------- | -------- | ------------- |
| `id`  | `string{:ts}` | Yes      | Challenge ID. |

**Response**

`<green>200 goodAdminChallenge</green>`: `AdminChallengeV2` object. See [`AdminChallengeV2`](#adminchallengev2).

**Errors**: `<red>401 badToken</red>`, `<red>403 badPerms</red>`, `<red>404 badChallenge</red>`.

### `<magenta>PUT</magenta> <black>/api/v2/admin/challs/:id</black>`

Creates or updates a challenge. Omitted fields in `data` keep their current value. Setting `instancerConfig{:ts}` or `adminBotConfig{:ts}` to `null{:ts}` clears the integration config.

| Property       | Value              |
| -------------- | ------------------ |
| Auth           | Required           |
| Gate           | None               |
| Permissions    | `challsWrite{:ts}` |
| Captcha action | —                  |
| Rate limit     | —                  |

**Path params**

| Field | Type          | Required | Notes                                 |
| ----- | ------------- | -------- | ------------------------------------- |
| `id`  | `string{:ts}` | Yes      | Challenge ID. Created if not present. |

**Request body**

```json
{
  "data": {
    "name": "web demo",
    "category": "web",
    "author": "organizers",
    "description": "Solve it.",
    "flag": "flag{example}",
    "points": { "min": 100, "max": 500 },
    "tiebreakEligible": true,
    "files": [{ "name": "dist.zip", "url": "/uploads/dist.zip", "size": 12345 }],
    "sortWeight": 10,
    "instancerConfig": null,
    "adminBotConfig": null,
    "hidden": false,
    "releaseTime": null
  }
}
```

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `data` | `Partial<AdminChallengeV2>{:ts}` | Yes | Partial challenge data. Omitted fields keep existing values. |

`instancerConfig{:ts}` is validated against the active instancer provider schema. `adminBotConfig{:ts}` source is validated by the active admin bot provider and stored with a generated revision. Updating a challenge triggers a leaderboard recalculation.

**Response**

`<green>200 goodChallengeUpdate</green>`: updated `AdminChallengeV2`.

**Errors**: `<red>400 badBody</red>`, `<red>401 badToken</red>`, `<red>403 badPerms</red>`, `<red>400 badInstancerConfig</red>`, `<red>400 badAdminBotConfig</red>`.

### `<red>DELETE</red> <black>/api/v2/admin/challs/:challengeId/solves/:userId</black>`

Deletes one solve for a specific user and challenge. Triggers a leaderboard recalculation.

| Property       | Value                   |
| -------------- | ----------------------- |
| Auth           | Required                |
| Gate           | None                    |
| Permissions    | `challsSolveWrite{:ts}` |
| Captcha action | —                       |
| Rate limit     | —                       |

**Path params**

| Field         | Type          | Required | Notes                                 |
| ------------- | ------------- | -------- | ------------------------------------- |
| `challengeId` | `string{:ts}` | Yes      | Challenge ID.                         |
| `userId`      | `string{:ts}` | Yes      | Team ID whose solve is being removed. |

**Response**

`<green>200 goodChallengeSolveDelete</green>` (no data).

**Errors**: `<red>401 badToken</red>`, `<red>403 badPerms</red>`, `<red>404 badUnknownSolve</red>`.

## Upload routes

### `<green>POST</green> <black>/api/v2/admin/upload</black>`

Uploads one or more files. Files are deduplicated by SHA-256 — uploading the same content twice returns the existing URL.

| Property       | Value              |
| -------------- | ------------------ |
| Auth           | Required           |
| Gate           | None               |
| Permissions    | `challsWrite{:ts}` |
| Captcha action | —                  |
| Rate limit     | —                  |

**Request**

`multipart/form-data` with one or more `files` form fields.

**Response**

`<green>200 goodFilesUpload</green>`: array of upload objects.

| Field  | Type                  | Notes                |
| ------ | --------------------- | -------------------- |
| `name` | `string{:ts}`         | Display filename.    |
| `url`  | `string{:ts}`         | Public download URL. |
| `size` | `number \| null{:ts}` | File size in bytes.  |

**Errors**: `<red>400 badBody</red>`, `<red>401 badToken</red>`, `<red>403 badPerms</red>`, `<red>500 badFilesUpload</red>`.

### `<green>POST</green> <black>/api/v2/admin/upload/query</black>`

Checks whether files already exist by SHA-256 without uploading them.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `challsRead{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Request body**

```json
{
  "uploads": [{ "sha256": "<hex-digest>", "name": "dist.zip" }]
}
```

| Field              | Type          | Required | Notes                       |
| ------------------ | ------------- | -------- | --------------------------- |
| `uploads[].sha256` | `string{:ts}` | Yes      | Hex-encoded SHA-256 digest. |
| `uploads[].name`   | `string{:ts}` | Yes      | Display filename.           |

**Response**

`<green>200 goodUploadsQuery</green>`: array with one entry per query — each entry has nullable `url` and nullable `size`. A `null{:ts}` URL means the file is not yet uploaded.

**Errors**: `<red>400 badBody</red>`, `<red>401 badToken</red>`, `<red>403 badPerms</red>`.

## Team routes

### `<cyan>GET</cyan> <black>/api/v2/admin/users</black>`

Lists teams. Query parameters control pagination, sorting, and search.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `usersWrite{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Query**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `limit` | `number{:ts}` | Yes | Integer from `1` through `100`. |
| `offset` | `number{:ts}` | Yes | Integer ≥ `0`. |
| `search` | `string{:ts}` | No | 1–100 characters. |
| `sortBy` | `AdminTeamSortBy{:ts}` | No | `createdAt`, `team`, `email`, `division`, `score`, `solves`, or `status`. |
| `sortOrder` | `SortOrder{:ts}` | No | `asc` or `desc`. |

**Response**

`<green>200 goodAdminUsers</green>`: `{ total: number, users: AdminUserListItem[] }`. Each list item includes ID, name, email, division, perms, banned state, score, solve count, avatar URL, country code, status text, and `createdAt`.

**Errors**: `<red>400 badBody</red>`, `<red>401 badToken</red>`, `<red>403 badPerms</red>`.

### `<green>POST</green> <black>/api/v2/admin/users</black>`

Lists teams with rich filters. Same query parameters as `<cyan>GET</cyan>`, with filter object in the body.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `usersWrite{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Query**

Identical to `<cyan>GET</cyan> <black>/api/v2/admin/users</black>`.

**Request body**

```json
{
  "status": { "include": ["active"], "exclude": ["banned"] },
  "division": { "include": ["open"] }
}
```

| Field      | Type                           | Required | Notes                        |
| ---------- | ------------------------------ | -------- | ---------------------------- |
| `status`   | `Filter<AdminTeamStatus>{:ts}` | No       | `active`, `banned`, `admin`. |
| `division` | `Filter<string>{:ts}`          | No       | Division keys.               |

Filter objects accept nullable `include` and `exclude` arrays.

**Response**

Same shape as `<cyan>GET</cyan> <black>/api/v2/admin/users</black>`.

**Errors**: `<red>400 badBody</red>`, `<red>401 badToken</red>`, `<red>403 badPerms</red>`.

### `<cyan>GET</cyan> <black>/api/v2/admin/users/:id</black>`

Returns full team details, including solve history.

| Property       | Value                                |
| -------------- | ------------------------------------ |
| Auth           | Required                             |
| Gate           | None                                 |
| Permissions    | `usersWrite{:ts}`, `challsRead{:ts}` |
| Captcha action | —                                    |
| Rate limit     | —                                    |

**Path params**

| Field | Type          | Required | Notes    |
| ----- | ------------- | -------- | -------- |
| `id`  | `string{:ts}` | Yes      | Team ID. |

**Response**

`<green>200 goodAdminUser</green>`:

| Field | Type | Notes |
| --- | --- | --- |
| `id` | `string{:ts}` | Team ID. |
| `name` | `string{:ts}` | Team name. |
| `email` | `string \| null{:ts}` | Team email. |
| `division` | `string{:ts}` | Division key. |
| `perms` | `number{:ts}` | Permission bitmask. |
| `banned` | `boolean{:ts}` | Whether the team is banned. |
| `score` | `number{:ts}` | Cached current score. |
| `solveCount` | `number{:ts}` | Cached solve count. |
| `avatarUrl` | `string \| null{:ts}` | Avatar URL. |
| `countryCode` | `string \| null{:ts}` | Country code. |
| `statusText` | `string \| null{:ts}` | Status text. |
| `createdAt` | `string{:ts}` | ISO timestamp. |
| `solves[]` | `{ challengeId, challengeName, challengeCategory, createdAt }[]{:ts}` | Solve history. |

**Errors**: `<red>401 badToken</red>`, `<red>403 badPerms</red>`, `<red>404 badUnknownUser</red>`.

### `<magenta>PUT</magenta> <black>/api/v2/admin/users/:id</black>`

Mutates team state. Admins cannot mutate users with admin permissions.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `usersWrite{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Path params**

| Field | Type          | Required | Notes    |
| ----- | ------------- | -------- | -------- |
| `id`  | `string{:ts}` | Yes      | Team ID. |

**Request body**

```json
{
  "data": { "banned": true }
}
```

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `data.banned` | `boolean{:ts}` | No | Banning removes the team from leaderboard output after recalculation without deleting solves. Unbanning restores rank after recalculation. |

**Response**

`<green>200 goodAdminUserUpdate</green>` (no data).

**Errors**: `<red>400 badBody</red>`, `<red>401 badToken</red>`, `<red>403 badPerms</red>`, `<red>404 badUnknownUser</red>`, `<red>403 badUserPrivileged</red>`.

### `<red>DELETE</red> <black>/api/v2/admin/users/:id</black>`

Deletes a team. Cannot delete users with admin permissions.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `usersWrite{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Path params**

| Field | Type          | Required | Notes    |
| ----- | ------------- | -------- | -------- |
| `id`  | `string{:ts}` | Yes      | Team ID. |

**Response**

`<green>200 goodAdminUserDelete</green>` (no data).

**Errors**: `<red>401 badToken</red>`, `<red>403 badPerms</red>`, `<red>404 badUnknownUser</red>`, `<red>403 badUserPrivileged</red>`.

### `<green>POST</green> <black>/api/v2/admin/users/:id/token</black>`

Generates a fresh long-lived team token. Useful for recovering teams that have lost access.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `usersWrite{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Path params**

| Field | Type          | Required | Notes    |
| ----- | ------------- | -------- | -------- |
| `id`  | `string{:ts}` | Yes      | Team ID. |

**Response**

`<green>200 goodCreateUserToken</green>`: `{ token: string }`.

**Errors**: `<red>401 badToken</red>`, `<red>403 badPerms</red>`, `<red>404 badUnknownUser</red>`, `<red>403 badUserPrivileged</red>`.

## Pending verification routes

Pending verification rows represent users who registered with an email but have not yet completed email verification.

### `<cyan>GET</cyan> <black>/api/v2/admin/user-verifications</black>`

Lists outstanding pending verifications.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `usersWrite{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Response**

`<green>200 goodAdminUserVerifications</green>`: `{ verifications: PendingUserVerification[] }`. Each row contains `id`, `name`, `email`, `division`, `createdAt`, and `expiresAt`.

**Errors**: `<red>401 badToken</red>`, `<red>403 badPerms</red>`.

### `<green>POST</green> <black>/api/v2/admin/user-verifications/:id/complete</black>`

Manually completes a pending verification, creating the team without requiring the user to follow the verification email link.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `usersWrite{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Path params**

| Field | Type          | Required | Notes                    |
| ----- | ------------- | -------- | ------------------------ |
| `id`  | `string{:ts}` | Yes      | Pending verification ID. |

**Response**

`<green>200 goodAdminUserVerificationComplete</green>`: `{ userId: string }`.

**Errors**: `<red>401 badToken</red>`, `<red>403 badPerms</red>`, `<red>404 badUnknownVerification</red>`.

### `<green>POST</green> <black>/api/v2/admin/user-verifications/:id/resend</black>`

Resends the verification email for an outstanding row.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `usersWrite{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Path params**

| Field | Type          | Required | Notes                    |
| ----- | ------------- | -------- | ------------------------ |
| `id`  | `string{:ts}` | Yes      | Pending verification ID. |

**Response**

`<green>200 goodAdminUserVerificationResend</green>`: `{ id: string }`.

**Errors**: `<red>401 badToken</red>`, `<red>403 badPerms</red>`, `<red>404 badUnknownVerification</red>`.

## Submission audit routes

The submissions table records every flag submission and admin bot job for compliance and abuse investigation. Each row is keyed by submission ID and includes the IP address.

### `<cyan>GET</cyan> <black>/api/v2/admin/submissions</black>`

Lists submission audit rows.

| Property       | Value                                |
| -------------- | ------------------------------------ |
| Auth           | Required                             |
| Gate           | None                                 |
| Permissions    | `usersWrite{:ts}`, `challsRead{:ts}` |
| Captcha action | —                                    |
| Rate limit     | —                                    |

**Query**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `limit` | `number{:ts}` | Yes | Integer from `1` through `100`. |
| `offset` | `number{:ts}` | Yes | Integer ≥ `0`. |
| `sortBy` | `SubmissionSortBy{:ts}` | No | `createdAt`, `challenge`, `team`, `ip`, `kind`, or `result`. |
| `sortOrder` | `SortOrder{:ts}` | No | `asc` or `desc`. |
| `challengeSearch` | `string{:ts}` | No | 1–100 characters. |
| `teamSearch` | `string{:ts}` | No | 1–100 characters. |

**Response**

`<green>200 goodAdminSubmissions</green>`: `{ total: number, submissions: AdminSubmission[] }`. Each row includes submission ID, kind, challenge metadata, user metadata, IP address, result, provider-specific details, optional `relatedId`, and `createdAt`.

**Errors**: `<red>400 badBody</red>`, `<red>401 badToken</red>`, `<red>403 badPerms</red>`.

### `<green>POST</green> <black>/api/v2/admin/submissions</black>`

Lists submission audit rows with rich filters.

| Property       | Value                                |
| -------------- | ------------------------------------ |
| Auth           | Required                             |
| Gate           | None                                 |
| Permissions    | `usersWrite{:ts}`, `challsRead{:ts}` |
| Captcha action | —                                    |
| Rate limit     | —                                    |

**Query**

Identical to `<cyan>GET</cyan> <black>/api/v2/admin/submissions</black>`.

**Request body**

```json
{
  "challenge": { "include": ["challenge-id"] },
  "team": { "include": ["team-id"] },
  "kind": { "include": ["flag", "admin_bot"] },
  "result": { "exclude": ["incorrect"] },
  "teamStatus": { "include": ["not_banned"] },
  "category": { "include": ["web"] },
  "division": { "include": ["open"] },
  "createdAfter": "2026-05-05T09:30:00.000Z",
  "createdBefore": "2026-05-05T10:30:00.000Z"
}
```

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `challenge` | `Filter<string>{:ts}` | No | Challenge IDs. |
| `team` | `Filter<string>{:ts}` | No | Team IDs. |
| `kind` | `Filter<SubmissionKind>{:ts}` | No | `flag`, `admin_bot`. |
| `result` | `Filter<SubmissionResult>{:ts}` | No | `correct`, `incorrect`, `already_solved`, `queued`, `active_job`, `invalid_input`, `bad_instancer_state`. |
| `teamStatus` | `Filter<SubmissionTeamStatus>{:ts}` | No | `banned`, `not_banned`. |
| `category` | `Filter<string>{:ts}` | No | Challenge categories. |
| `division` | `Filter<string>{:ts}` | No | Division keys. |
| `createdAfter` | `string{:ts}` | No | ISO timestamp lower bound. |
| `createdBefore` | `string{:ts}` | No | ISO timestamp upper bound. |

**Response**

Same shape as `<cyan>GET</cyan> <black>/api/v2/admin/submissions</black>`.

**Errors**: `<red>400 badBody</red>`, `<red>401 badToken</red>`, `<red>403 badPerms</red>`.

## Settings routes

Runtime settings override values from `rctf.d/*.yaml` config files without restarting the server. Setting a field to `null{:ts}` clears the override.

### `<cyan>GET</cyan> <black>/api/v2/admin/settings</black>`

Returns the current overrides and defaults side-by-side.

| Property       | Value                |
| -------------- | -------------------- |
| Auth           | Required             |
| Gate           | None                 |
| Permissions    | `settingsWrite{:ts}` |
| Captcha action | —                    |
| Rate limit     | —                    |

**Response**

`<green>200 goodAdminSettings</green>`: `{ overrides: AdminSettings, defaults: AdminSettings }`. `overrides` are runtime values from the database; `defaults` come from config files and environment variables.

**Errors**: `<red>401 badToken</red>`, `<red>403 badPerms</red>`.

### `<magenta>PUT</magenta> <black>/api/v2/admin/settings</black>`

Updates runtime overrides. Updating `startTime` or `endTime` forces a leaderboard recalculation.

| Property       | Value                |
| -------------- | -------------------- |
| Auth           | Required             |
| Gate           | None                 |
| Permissions    | `settingsWrite{:ts}` |
| Captcha action | —                    |
| Rate limit     | —                    |

**Request body**

```json
{
  "data": {
    "ctfName": "Example CTF",
    "homeContent": "# Welcome",
    "startTime": 1770000000000,
    "endTime": 1770100000000,
    "sponsors": [
      {
        "name": "Example",
        "icon": "https://example.com/icon.png",
        "description": "Sponsor",
        "url": "https://example.com"
      }
    ],
    "meta": { "description": "Example CTF", "imageUrl": "https://example.com/card.png" },
    "faviconUrl": null,
    "logoLightUrl": null,
    "logoDarkUrl": null
  }
}
```

| Field  | Type                          | Required | Notes                            |
| ------ | ----------------------------- | -------- | -------------------------------- |
| `data` | `Partial<AdminSettings>{:ts}` | Yes      | Nullable values clear overrides. |

**Response**

`<green>200 goodAdminSettingsUpdate</green>`: same shape as the `<cyan>GET</cyan>` response (returns the updated overrides and defaults).

**Errors**: `<red>400 badBody</red>`, `<red>401 badToken</red>`, `<red>403 badPerms</red>`.

## Schema routes

### `<cyan>GET</cyan> <black>/api/v2/admin/instancer/schema</black>`

Returns the active instancer provider's JSON schema. Used by the challenge editor to validate `instancerConfig{:ts}`.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `challsRead{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Response**

`<green>200 goodInstancerSchema</green>`: `{ schema: Record<string, unknown>, defaults: Record<string, unknown> }`.

**Errors**: `<red>401 badToken</red>`, `<red>403 badPerms</red>`, `<red>404 badEndpoint</red>` (no instancer provider configured).

### `<cyan>GET</cyan> <black>/api/v2/admin/admin-bot/status</black>`

Returns admin bot enablement and source language.

| Property       | Value             |
| -------------- | ----------------- |
| Auth           | Required          |
| Gate           | None              |
| Permissions    | `challsRead{:ts}` |
| Captcha action | —                 |
| Rate limit     | —                 |

**Response**

`<green>200 goodAdminBotStatus</green>`: `{ enabled: boolean, configLanguage: string }`.

**Errors**: `<red>401 badToken</red>`, `<red>403 badPerms</red>`.

## Admin bot service routes

These routes are not user-admin endpoints. They use the shared admin bot bearer token (`adminBot.provider.options.secretKey{:yaml}` in `rctf.d/`) and are intended for the admin bot worker only.

### `<green>POST</green> <black>/api/v2/admin/admin-bot/jobs/pull</black>`

Claims the oldest queued job for processing.

| Property       | Value   |
| -------------- | ------- |
| Auth           | Service |
| Gate           | None    |
| Permissions    | —       |
| Captcha action | —       |
| Rate limit     | —       |

**Response**

`<green>200 goodAdminBotJobPull</green>`: `{ job: PulledAdminBotJob \| null }`. When no job is queued, returns `{ job: null }`.

A pulled job contains `id`, `challengeId`, `configRevision`, `userId`, `submittedAt`, `flag`, submitted `inputs`, and `instancerInstances`.

**Errors**: `<red>401 badToken</red>`.

### `<cyan>GET</cyan> <black>/api/v2/admin/admin-bot/challenges/:id/source</black>`

Fetches the challenge source code for a specific config revision.

| Property       | Value   |
| -------------- | ------- |
| Auth           | Service |
| Gate           | None    |
| Permissions    | —       |
| Captcha action | —       |
| Rate limit     | —       |

**Path params**

| Field | Type          | Required | Notes         |
| ----- | ------------- | -------- | ------------- |
| `id`  | `string{:ts}` | Yes      | Challenge ID. |

**Response**

`<green>200 goodAdminBotChallengeSource</green>`: `{ sourceCode: string, configRevision: string }`.

**Errors**: `<red>401 badToken</red>`, `<red>404 badChallenge</red>`.

### `<green>POST</green> <black>/api/v2/admin/admin-bot/jobs/:id/complete</black>`

Marks a running job as completed and stores logs.

| Property       | Value   |
| -------------- | ------- |
| Auth           | Service |
| Gate           | None    |
| Permissions    | —       |
| Captcha action | —       |
| Rate limit     | —       |

**Path params**

| Field | Type          | Required | Notes   |
| ----- | ------------- | -------- | ------- |
| `id`  | `string{:ts}` | Yes      | Job ID. |

**Request body**

```json
{
  "logs": "newline-delimited log data"
}
```

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `logs` | `string \| undefined{:ts}` | No | Newline-delimited JSON. Maximum `1048576` characters. |

**Response**

`<green>200 goodAdminBotJobUpdate</green>`: `{ ok: boolean }`.

**Errors**: `<red>400 badBody</red>`, `<red>401 badToken</red>`.

### `<green>POST</green> <black>/api/v2/admin/admin-bot/jobs/:id/fail</black>`

Marks a running job as failed and stores logs.

| Property       | Value   |
| -------------- | ------- |
| Auth           | Service |
| Gate           | None    |
| Permissions    | —       |
| Captcha action | —       |
| Rate limit     | —       |

**Path params**

| Field | Type          | Required | Notes   |
| ----- | ------------- | -------- | ------- |
| `id`  | `string{:ts}` | Yes      | Job ID. |

**Request body**

Same shape as `complete`.

**Response**

`<green>200 goodAdminBotJobUpdate</green>`: `{ ok: boolean }`.

**Errors**: `<red>400 badBody</red>`, `<red>401 badToken</red>`.

### `<cyan>GET</cyan> <black>/api/v2/admin/admin-bot/queue-depth</black>`

Returns the number of queued jobs.

| Property       | Value   |
| -------------- | ------- |
| Auth           | Service |
| Gate           | None    |
| Permissions    | —       |
| Captcha action | —       |
| Rate limit     | —       |

**Response**

`<green>200 goodAdminBotQueueDepth</green>`: `{ depth: number }`.

**Errors**: `<red>401 badToken</red>`.

## V1 compatibility

### Challenges

#### `<cyan>GET</cyan> <black>/api/v1/admin/challs</black>`

Same as [`<cyan>GET</cyan> <black>/api/v2/admin/challs</black>`](#get-apiv2adminchalls), but V1 admin challenge data omits `hidden`, `releaseTime`, `instancerConfig`, `adminBotConfig`, and file `size`.

#### `<magenta>PUT</magenta> <black>/api/v1/admin/challs/:id</black>`

Same as [`<magenta>PUT</magenta> <black>/api/v2/admin/challs/:id</black>`](#put-apiv2adminchallsid), with the V1 challenge data shape (no `hidden`, `releaseTime`, `instancerConfig`, `adminBotConfig`, or file `size`).

#### `<red>DELETE</red> <black>/api/v1/admin/challs/:id</black>`

V1-only. Deletes a challenge and all its solves. Triggers a leaderboard recalculation. No V2 equivalent — V2 deletes via challenge mutation in the admin panel.

| Property       | Value              |
| -------------- | ------------------ |
| Auth           | Required           |
| Gate           | None               |
| Permissions    | `challsWrite{:ts}` |
| Captcha action | —                  |
| Rate limit     | —                  |

**Path params**

| Field | Type          | Required | Notes         |
| ----- | ------------- | -------- | ------------- |
| `id`  | `string{:ts}` | Yes      | Challenge ID. |

**Response**

`<green>200 goodChallengeDelete</green>` (no data).

**Errors**: `<red>401 badToken</red>`, `<red>403 badPerms</red>`, `<red>404 badChallenge</red>`.

### Uploads

#### `<green>POST</green> <black>/api/v1/admin/upload</black>`

Differs from V2: accepts JSON with base64-encoded data URIs instead of `multipart/form-data`.

**Request body**

```json
{
  "files": [{ "name": "dist.zip", "data": "data:application/zip;base64,..." }]
}
```

V1 upload responses omit the `size` field.

**Errors**: `<red>400 badBody</red>`, `<red>401 badToken</red>`, `<red>403 badPerms</red>`, `<red>400 badDataUri</red>`, `<red>500 badFilesUpload</red>`.

#### `<green>POST</green> <black>/api/v1/admin/upload/query</black>`

Same as [`<green>POST</green> <black>/api/v2/admin/upload/query</black>`](#post-apiv2adminuploadquery), but V1 upload query responses include nullable `url` only — no `size`.

## `AdminChallengeV2`

| Field | Type | Notes |
| --- | --- | --- |
| `id` | `string{:ts}` | Challenge ID. |
| `name` | `string{:ts}` | Display name. |
| `description` | `string{:ts}` | Markdown description. |
| `category` | `string{:ts}` | Category name. |
| `author` | `string{:ts}` | Author. |
| `files` | `ChallengeFileV2[]{:ts}` | Files. See [`ChallengeFileV2`](/api/responses/#challengefilev2). |
| `points` | `{ min: number, max: number }{:ts}` | Dynamic scoring bounds. |
| `flag` | `string{:ts}` | Correct flag. |
| `tiebreakEligible` | `boolean{:ts}` | Whether solves count toward the tiebreak timestamp. |
| `sortWeight` | `number \| null \| undefined{:ts}` | Optional category ordering weight. |
| `instancerConfig` | `InstancerConfig \| null \| undefined{:ts}` | Per-team instance config. See [`InstancerConfig`](/api/responses/#instancerconfig). |
| `adminBotConfig` | `AdminBotConfig \| null \| undefined{:ts}` | Admin bot config. See [`AdminBotConfig`](/api/responses/#adminbotconfig). |
| `hidden` | `boolean{:ts}` | Hidden challenges are invisible to non-admin users. |
| `releaseTime` | `number \| null \| undefined{:ts}` | Unix millisecond release timestamp. |
