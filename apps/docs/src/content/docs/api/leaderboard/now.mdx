---
title: '<route>GET</route> Current standings'
description: '<route>GET /api/[v2,v1]/leaderboard/now</route>'
---

import { GetLeaderboardRoute, GetLeaderboardRouteV2 } from '@rctf/types'

<RouteAside>
  <Tabs items={['V2', 'V1']} sync="leaderboard-now-version">
    <Tab>
      <RouteExample def={GetLeaderboardRouteV2} query={{ limit: 50, offset: 0 }} />
    </Tab>
    <Tab>
      <RouteExample def={GetLeaderboardRoute} query={{ limit: 50, offset: 0 }} />
    </Tab>
  </Tabs>
</RouteAside>

<RouteMetaFromDef
  def={GetLeaderboardRouteV2}
  rateLimit="Search bucket per IP (burst `3`, refill window `3000` ms) when `search` is provided."
/>

Returns a page of the current standings from the leaderboard cache.

For new clients, prefer V2. It includes avatar, country, status, solve, division rank, and global rank fields. V1 remains available for older clients and returns the original `id`, `name`, and `score` fields.

<Tabs items={['V2', 'V1']} sync="leaderboard-now-version">
  <Tab>

`<route>GET /api/v2/leaderboard/now</route>` supports `limit`, `offset`, optional `division`, and optional `search`. Deployment config sets the maximum pagination values, and search values are expected to be 2 to 100 characters.

<RequestBody def={GetLeaderboardRouteV2} source="query" title="Query parameters" />

  </Tab>

  <Tab>

`<route>GET /api/v1/leaderboard/now</route>` supports `limit`, `offset`, and optional `division`. Team name search is available through the V2 route.

<RequestBody def={GetLeaderboardRoute} source="query" title="Query parameters" />

  </Tab>
</Tabs>

<Tabs items={['V2', 'V1']} sync="leaderboard-now-version">
  <Tab>
    <ResponseBody def={GetLeaderboardRouteV2} response="goodLeaderboard" title="Response fields" />
  </Tab>
  <Tab>
    <ResponseBody def={GetLeaderboardRoute} response="goodLeaderboard" title="Response fields" />
  </Tab>
</Tabs>

`globalPlace` is `null{:ts}` when a team is not included in global ranking. `divisionPlace` is the team's rank within the returned division.
