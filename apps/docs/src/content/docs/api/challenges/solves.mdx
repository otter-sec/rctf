---
title: '<route>GET</route> Challenge solves'
description: '<route>GET /api/[v2,v1]/challs/:id/solves</route>'
---

import { GetChallengeSolvesRoute, GetChallengeSolvesRouteV2 } from '@rctf/types'

<RouteAside>
  <Tabs items={['V2', 'V1']} sync="solves-version">
    <Tab>
      <RouteExample def={GetChallengeSolvesRouteV2} query={{ limit: 100, offset: 0 }} />
    </Tab>
    <Tab>
      <RouteExample def={GetChallengeSolvesRoute} query={{ limit: 100, offset: 0 }} />
    </Tab>
  </Tabs>
</RouteAside>

<RouteMetaFromDef def={GetChallengeSolvesRouteV2} />

Returns a page of solve history for one challenge.

V2 includes solve avatars, country codes, division placements, blood index, and the authenticated user's `mySolvePosition` when optional auth is present. V1 remains available for older clients and returns a smaller solve row.

<RequestBody def={GetChallengeSolvesRouteV2} source="params" title="Path parameters" />

<RequestBody def={GetChallengeSolvesRouteV2} source="query" title="Query parameters" />

<Tabs items={['V2', 'V1']} sync="solves-version">
  <Tab>
    <ResponseBody
      def={GetChallengeSolvesRouteV2}
      response="goodChallengeSolves"
      title="Response fields"
    />
  </Tab>
  <Tab>
    <ResponseBody
      def={GetChallengeSolvesRoute}
      response="goodChallengeSolves"
      title="Response fields"
    />
  </Tab>
</Tabs>
