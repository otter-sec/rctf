---
title: Integrations
description: API reference for client config, analytics, CTFtime, instancer lifecycle, and admin bot participant routes.
---

Integration routes expose frontend configuration, CTFtime handoff, challenge instance lifecycle, admin bot participant jobs, and the analytics script proxy.

## Routes

| Method | Path | Version | Auth | Gate |
| --- | --- | --- | --- | --- |
| `<route>GET</route>` | `<route>/api/v2/integrations/client/config</route>` | V2 | Public | None |
| `<route>GET</route>` | `<route>/api/v2/integrations/analytics/script</route>` | V2 | Public | None |
| `<route>GET</route>` | `<route>/api/v2/integrations/challs/:id/instance</route>` | V2 | Required | Started |
| `<route>PUT</route>` | `<route>/api/v2/integrations/challs/:id/instance</route>` | V2 | Required | Started |
| `<route>PATCH</route>` | `<route>/api/v2/integrations/challs/:id/instance</route>` | V2 | Required | Started |
| `<route>DELETE</route>` | `<route>/api/v2/integrations/challs/:id/instance</route>` | V2 | Required | Started |
| `<route>GET</route>` | `<route>/api/v2/integrations/challs/:id/admin-bot/config</route>` | V2 | Public | Started |
| `<route>POST</route>` | `<route>/api/v2/integrations/challs/:id/admin-bot</route>` | V2 | Required | Started |
| `<route>GET</route>` | `<route>/api/v2/integrations/challs/:id/admin-bot/status</route>` | V2 | Required | Started |
| `<route>GET</route>` | `<route>/api/v2/integrations/challs/:id/admin-bot/history</route>` | V2 | Required | Started |
| `<route>GET</route>` | `<route>/api/v2/integrations/challs/:id/admin-bot/jobs/:jobId/logs</route>` | V2 | Required | Started |
| `<route>GET</route>` | `<route>/api/v1/integrations/client/config</route>` | V1 | Public | None |
| `<route>POST</route>` | `<route>/api/v1/integrations/ctftime/callback</route>` | V1 | Public | None |
| `<route>GET</route>` | `<route>/api/v1/integrations/ctftime/leaderboard</route>` | V1 | Permissioned | None |

## Client config

### `<route>GET /api/v2/integrations/client/config</route>`

Returns the public client configuration consumed by the frontend.

| Property       | Value  |
| -------------- | ------ |
| Auth           | Public |
| Gate           | None   |
| Permissions    | -      |
| Captcha action | -      |
| Rate limit     | -      |

**Response**

`<response>200 goodClientConfig</response>`:

```json
{
  "kind": "goodClientConfig",
  "message": "The client config was retrieved.",
  "data": {}
}
```

| Field | Type | Notes |
| --- | --- | --- |
| `meta` | `{ description: string, imageUrl: string }{:ts}` | Page metadata. |
| `homeContent` | `string{:ts}` | Markdown home content. |
| `sponsors` | `Sponsor[]{:ts}` | Sponsor rows. |
| `flagFormatPlaceholder` | `string{:ts}` | Placeholder shown in flag inputs. |
| `analytics` | `ClientProvider \| null{:ts}` | Public analytics provider data. |
| `ctfName` | `string{:ts}` | Display name. |
| `divisions` | `Record<string, string>{:ts}` | Division key to display name map. |
| `defaultDivision` | `string \| null \| undefined{:ts}` | Default division key when configured. |
| `origin` | `string{:ts}` | Public origin URL. |
| `startTime` | `number{:ts}` | Unix milliseconds. |
| `endTime` | `number{:ts}` | Unix milliseconds. |
| `userMembers` | `boolean{:ts}` | Whether team members are enabled. |
| `faviconUrl` | `string \| null{:ts}` | Runtime favicon URL. |
| `logoLightUrl` | `string \| null{:ts}` | Light logo URL. |
| `logoDarkUrl` | `string \| null{:ts}` | Dark logo URL. |
| `emailEnabled` | `boolean{:ts}` | Whether email delivery is configured. |
| `registrationsEnabled` | `boolean \| null{:ts}` | Runtime registration state. |
| `ctftime` | `{ clientId: string } \| null{:ts}` | Public CTFtime OAuth data. |
| `instancerEnabled` | `boolean{:ts}` | Whether an instancer provider is configured. |
| `isArchived` | `boolean{:ts}` | Whether the CTF has ended. |
| `captcha` | `CaptchaClientConfig \| null{:ts}` | Public captcha provider options and protected endpoint map. |

### `<route>GET /api/v1/integrations/client/config</route>`

V1 client config. Differs from V2:

- Omits `flagFormatPlaceholder`, `analytics`, `instancerEnabled`, `isArchived`, `logoLightUrl`, `logoDarkUrl`, and the generic `captcha` object.
- Includes `globalSiteTag` and `recaptcha`.

| Property       | Value  |
| -------------- | ------ |
| Auth           | Public |
| Gate           | None   |
| Permissions    | -      |
| Captcha action | -      |
| Rate limit     | -      |

## Analytics script

### `<route>GET /api/v2/integrations/analytics/script</route>`

Returns the active analytics provider's JavaScript body. This route is not part of `@rctf/types`.

| Property       | Value  |
| -------------- | ------ |
| Auth           | Public |
| Gate           | None   |
| Permissions    | -      |
| Captcha action | -      |
| Rate limit     | -      |

**Response**

The route returns the provider's JavaScript body or a plain text error. It is not wrapped in the rCTF envelope. The API caches the script in memory for one hour. When no analytics provider is configured the route returns `404`. Upstream fetch failures return `502`.

## CTFtime

### `<route>POST /api/v1/integrations/ctftime/callback</route>`

V1-only. Exchanges a CTFtime OAuth code for a CTFtime auth token plus the team name and ID returned by CTFtime.

| Property       | Value  |
| -------------- | ------ |
| Auth           | Public |
| Gate           | None   |
| Permissions    | -      |
| Captcha action | -      |
| Rate limit     | -      |

**Request body**

```json
{
  "ctftimeCode": "<dim><oauth-code></dim>"
}
```

| Field         | Type          | Required | Notes                           |
| ------------- | ------------- | -------- | ------------------------------- |
| `ctftimeCode` | `string{:ts}` | Yes      | OAuth code returned by CTFtime. |

**Response**

`<response>200 goodCtftimeToken</response>`:

```json
{
  "kind": "goodCtftimeToken",
  "message": "The CTFtime token was retrieved.",
  "data": {
    "ctftimeToken": "<dim><ctftime-auth-token></dim>",
    "ctftimeName": "Team Name",
    "ctftimeId": "12345"
  }
}
```

| Field | Type | Notes |
| --- | --- | --- |
| `data.ctftimeToken` | `string{:ts}` | CTFtime auth token usable on V1 login and V2 register/verify flows. |
| `data.ctftimeName` | `string{:ts}` | CTFtime team name. |
| `data.ctftimeId` | `string{:ts}` | CTFtime team ID. |

**Errors**: `<response>400 badBody</response>`, `<response>401 badCtftimeCode</response>`, `<response>404 badEndpoint</response>`.

### `<route>GET /api/v1/integrations/ctftime/leaderboard</route>`

V1-only. Returns the CTFtime-formatted leaderboard. This is the only typed route that returns its data directly without the rCTF envelope.

| Property       | Value                  |
| -------------- | ---------------------- |
| Auth           | Permissioned           |
| Gate           | None                   |
| Permissions    | `leaderboardRead{:ts}` |
| Captcha action | -                      |
| Rate limit     | -                      |

**Response**

Raw body (200):

```json
{
  "standings": [
    {
      "pos": 1,
      "team": "otter-sec",
      "score": 1200
    }
  ]
}
```

| Field       | Type                                                  | Notes                   |
| ----------- | ----------------------------------------------------- | ----------------------- |
| `standings` | `{ pos: number, team: string, score: number }[]{:ts}` | Ordered standings rows. |

## Instancer lifecycle

All four methods share the path `<route>/api/v2/integrations/challs/:id/instance</route>` and the same path params. The route returns `<response>404 badEndpoint</response>` when no instancer provider is configured or the challenge has no instancer config. Provider failures return `<response>400 badInstancerError</response>` with `data.message`. Admins with `challsRead` can bypass the start-time gate.

### `<route>GET /api/v2/integrations/challs/:id/instance</route>`

Returns the current instance status for the authenticated user.

| Property       | Value                                           |
| -------------- | ----------------------------------------------- |
| Auth           | Required                                        |
| Gate           | Started                                         |
| Permissions    | `challsRead{:ts}` bypasses the start-time gate. |
| Captcha action | -                                               |
| Rate limit     | -                                               |

**Path params**

| Field | Type          | Required | Notes         |
| ----- | ------------- | -------- | ------------- |
| `id`  | `string{:ts}` | Yes      | Challenge ID. |

**Response**

`<response>200 goodInstanceStatus</response>`:

```json
{
  "kind": "goodInstanceStatus",
  "message": "The instance status was retrieved.",
  "data": {
    "status": "running",
    "timeLeftMilliseconds": 1800000,
    "endpoints": []
  }
}
```

| Field | Type | Notes |
| --- | --- | --- |
| `data.status` | `"stopped" \| "running" \| "starting" \| "stopping" \| "errored"{:ts}` | Current lifecycle state. |
| `data.timeLeftMilliseconds` | `number \| null{:ts}` | Remaining time for running instances. |
| `data.endpoints` | `Endpoint[] \| null{:ts}` | Public endpoints in challenge expose order. |

`Endpoint` has `kind`, `host`, `port`, and optional `title`. Endpoint kinds are `tcp`, `tcp-ssl`, `http`, and `https`.

**Errors**: `<response>404 badEndpoint</response>`, `<response>400 badInstancerError</response>`.

### `<route>PUT /api/v2/integrations/challs/:id/instance</route>`

Creates (starts) an instance for the authenticated user.

| Property       | Value                                           |
| -------------- | ----------------------------------------------- |
| Auth           | Required                                        |
| Gate           | Started                                         |
| Permissions    | `challsRead{:ts}` bypasses the start-time gate. |
| Captcha action | `instancerStart{:ts}` (when configured)         |
| Rate limit     | -                                               |

**Path params**

| Field | Type          | Required | Notes         |
| ----- | ------------- | -------- | ------------- |
| `id`  | `string{:ts}` | Yes      | Challenge ID. |

**Request body**

```json
{
  "captchaCode": "optional-captcha-code"
}
```

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `captchaCode` | `string{:ts}` | No | Checked only when captcha protects `instancerStart{:ts}`. |

**Response**

`<response>200 goodInstanceStatus</response>`. See [GET response](#get-apiv2integrationschallsidinstance) for the data shape.

**Errors**: `<response>400 badBody</response>`, `<response>404 badEndpoint</response>`, `<response>403 badCaptcha</response>`, `<response>400 badInstancerError</response>`.

### `<route>PATCH /api/v2/integrations/challs/:id/instance</route>`

Extends the running instance for the authenticated user.

| Property       | Value                                           |
| -------------- | ----------------------------------------------- |
| Auth           | Required                                        |
| Gate           | Started                                         |
| Permissions    | `challsRead{:ts}` bypasses the start-time gate. |
| Captcha action | `instancerExtend{:ts}` (when configured)        |
| Rate limit     | -                                               |

**Path params**

| Field | Type          | Required | Notes         |
| ----- | ------------- | -------- | ------------- |
| `id`  | `string{:ts}` | Yes      | Challenge ID. |

**Request body**

```json
{
  "captchaCode": "optional-captcha-code"
}
```

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `captchaCode` | `string{:ts}` | No | Checked only when captcha protects `instancerExtend{:ts}`. |

**Response**

`<response>200 goodInstanceStatus</response>`. See [GET response](#get-apiv2integrationschallsidinstance) for the data shape.

**Errors**: `<response>400 badBody</response>`, `<response>404 badEndpoint</response>`, `<response>403 badCaptcha</response>`, `<response>400 badInstancerError</response>`.

### `<route>DELETE /api/v2/integrations/challs/:id/instance</route>`

Stops the instance for the authenticated user.

| Property       | Value                                           |
| -------------- | ----------------------------------------------- |
| Auth           | Required                                        |
| Gate           | Started                                         |
| Permissions    | `challsRead{:ts}` bypasses the start-time gate. |
| Captcha action | -                                               |
| Rate limit     | -                                               |

**Path params**

| Field | Type          | Required | Notes         |
| ----- | ------------- | -------- | ------------- |
| `id`  | `string{:ts}` | Yes      | Challenge ID. |

**Response**

`<response>200 goodInstanceStatus</response>`. See [GET response](#get-apiv2integrationschallsidinstance) for the data shape.

**Errors**: `<response>404 badEndpoint</response>`, `<response>400 badInstancerError</response>`.

## Admin bot participant routes

Participant admin bot routes live under `<route>/api/v2/integrations/challs/:id/admin-bot/*</route>`. Admins with `challsRead` can bypass the start-time gate.

### `<route>GET /api/v2/integrations/challs/:id/admin-bot/config</route>`

Returns the released admin bot challenge source and its file extension.

| Property       | Value                                           |
| -------------- | ----------------------------------------------- |
| Auth           | Public                                          |
| Gate           | Started                                         |
| Permissions    | `challsRead{:ts}` bypasses the start-time gate. |
| Captcha action | -                                               |
| Rate limit     | -                                               |

**Path params**

| Field | Type          | Required | Notes         |
| ----- | ------------- | -------- | ------------- |
| `id`  | `string{:ts}` | Yes      | Challenge ID. |

**Response**

`<response>200 goodAdminBotConfig</response>`:

```json
{
  "kind": "goodAdminBotConfig",
  "message": "The admin bot config was retrieved.",
  "data": {
    "sourceCode": "export const challenge = ...",
    "fileExtension": ".ts"
  }
}
```

| Field                | Type          | Notes                                                  |
| -------------------- | ------------- | ------------------------------------------------------ |
| `data.sourceCode`    | `string{:ts}` | Released admin bot challenge source.                   |
| `data.fileExtension` | `string{:ts}` | File extension corresponding to the source's language. |

### `<route>POST /api/v2/integrations/challs/:id/admin-bot</route>`

Submits a participant admin bot job. Participants supply values for the configured input names.

| Property       | Value                                                       |
| -------------- | ----------------------------------------------------------- |
| Auth           | Required                                                    |
| Gate           | Started                                                     |
| Permissions    | `challsRead{:ts}` bypasses the start-time gate.             |
| Captcha action | `adminBotSubmit{:ts}` (when configured)                     |
| Rate limit     | Burst `1`, refill window `10000` ms per user and challenge. |

**Path params**

| Field | Type          | Required | Notes         |
| ----- | ------------- | -------- | ------------- |
| `id`  | `string{:ts}` | Yes      | Challenge ID. |

**Request body**

```json
{
  "inputs": {
    "url": "https://challenge.example.com/"
  },
  "captchaCode": "optional-captcha-code"
}
```

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `inputs` | `Record<string, string>{:ts}` | Yes | Values for the configured input names. Input names may be up to `256` characters. Input values may be up to `1024` characters. Each value is validated against the regex rule stored in challenge config. |
| `captchaCode` | `string{:ts}` | No | Checked only when captcha protects `adminBotSubmit{:ts}`. |

Only one queued or running job per user per challenge is allowed.

**Response**

`<response>200 goodAdminBotJobSubmitted</response>`:

```json
{
  "kind": "goodAdminBotJobSubmitted",
  "message": "The admin bot job was submitted.",
  "data": {
    "jobId": "job-id"
  }
}
```

| Field        | Type          | Notes                     |
| ------------ | ------------- | ------------------------- |
| `data.jobId` | `string{:ts}` | Submitted job identifier. |

The route returns `<response>400 badInstancerState</response>` when the challenge requires a running instance and the current user's instance is stopped, missing, or expires before the admin bot timeout.

**Errors**: `<response>400 badBody</response>`, `<response>403 badCaptcha</response>`, `<response>429 badRateLimit</response>`, `<response>400 badInstancerState</response>`.

### `<route>GET /api/v2/integrations/challs/:id/admin-bot/status</route>`

Returns the current active job for the authenticated user on that challenge.

| Property       | Value                                           |
| -------------- | ----------------------------------------------- |
| Auth           | Required                                        |
| Gate           | Started                                         |
| Permissions    | `challsRead{:ts}` bypasses the start-time gate. |
| Captcha action | -                                               |
| Rate limit     | -                                               |

**Path params**

| Field | Type          | Required | Notes         |
| ----- | ------------- | -------- | ------------- |
| `id`  | `string{:ts}` | Yes      | Challenge ID. |

**Response**

`<response>200 goodAdminBotJobStatus</response>`:

```json
{
  "kind": "goodAdminBotJobStatus",
  "message": "The admin bot job status was retrieved.",
  "data": {
    "job": {
      "id": "job-id",
      "status": "queued",
      "createdAt": "2026-05-05T10:00:00.000Z",
      "queuePosition": 2,
      "logs": null
    }
  }
}
```

| Field | Type | Notes |
| --- | --- | --- |
| `data.job` | `AdminBotJobStatusResponse \| null{:ts}` | `null{:ts}` when the user has no active job for that challenge. |
| `data.job.status` | `"queued" \| "running" \| "completed" \| "failed"{:ts}` | Job lifecycle state. |

### `<route>GET /api/v2/integrations/challs/:id/admin-bot/history</route>`

Returns retained completed and failed jobs for the authenticated user on that challenge.

| Property       | Value                                           |
| -------------- | ----------------------------------------------- |
| Auth           | Required                                        |
| Gate           | Started                                         |
| Permissions    | `challsRead{:ts}` bypasses the start-time gate. |
| Captcha action | -                                               |
| Rate limit     | -                                               |

**Path params**

| Field | Type          | Required | Notes         |
| ----- | ------------- | -------- | ------------- |
| `id`  | `string{:ts}` | Yes      | Challenge ID. |

**Response**

`<response>200 goodAdminBotJobHistory</response>`:

```json
{
  "kind": "goodAdminBotJobHistory",
  "message": "The admin bot job history was retrieved.",
  "data": {
    "jobs": [
      {
        "id": "job-id",
        "status": "completed",
        "createdAt": "2026-05-05T10:00:00.000Z",
        "hasLogs": true
      }
    ]
  }
}
```

| Field       | Type                            | Notes                               |
| ----------- | ------------------------------- | ----------------------------------- |
| `data.jobs` | `AdminBotJobHistoryItem[]{:ts}` | Retained completed and failed jobs. |

### `<route>GET /api/v2/integrations/challs/:id/admin-bot/jobs/:jobId/logs</route>`

Returns logs for a specific job.

| Property       | Value                                           |
| -------------- | ----------------------------------------------- |
| Auth           | Required                                        |
| Gate           | Started                                         |
| Permissions    | `challsRead{:ts}` bypasses the start-time gate. |
| Captcha action | -                                               |
| Rate limit     | -                                               |

**Path params**

| Field   | Type          | Required | Notes         |
| ------- | ------------- | -------- | ------------- |
| `id`    | `string{:ts}` | Yes      | Challenge ID. |
| `jobId` | `string{:ts}` | Yes      | Job ID.       |

**Response**

`<response>200 goodAdminBotJobLogs</response>`:

```json
{
  "kind": "goodAdminBotJobLogs",
  "message": "The admin bot job logs were retrieved.",
  "data": {
    "logs": null
  }
}
```

| Field       | Type                  | Notes                                                |
| ----------- | --------------------- | ---------------------------------------------------- |
| `data.logs` | `string \| null{:ts}` | Logs body, or `null{:ts}` when no logs are retained. |

## Service routes

The admin bot worker service routes live under `<route>/api/v2/admin/admin-bot/*</route>` and are documented in [Admin](/api/admin/#admin-bot-service-routes).
