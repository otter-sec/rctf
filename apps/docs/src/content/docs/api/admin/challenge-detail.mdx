---
title: '<route>GET</route> Admin challenge detail'
description: '<route>GET /api/[v2,v1]/admin/challs/:id</route>'
---

import { GetAdminChallengeRoute, GetAdminChallengeRouteV2 } from '@rctf/types'

<RouteAside>
  <Tabs items={['V2', 'V1']} sync="admin-challenge-detail-version">
    <Tab>
      <RouteExample def={GetAdminChallengeRouteV2} params={{ id: 'challenge-id' }} />
    </Tab>
    <Tab>
      <RouteExample def={GetAdminChallengeRoute} params={{ id: 'challenge-id' }} />
    </Tab>
  </Tabs>
</RouteAside>

<RouteMetaFromDef def={GetAdminChallengeRouteV2} />

This route reads one challenge with admin fields. The admin panel uses it when someone opens the challenge editor.

For new clients, V2 is usually the best fit. V1 returns the older admin challenge fields and remains available for existing integrations.

<Tabs items={['V2', 'V1']} sync="admin-challenge-detail-version">
  <Tab>
    <RequestBody def={GetAdminChallengeRouteV2} source="params" title="Path parameters" />
  </Tab>
  <Tab>
    <RequestBody def={GetAdminChallengeRoute} source="params" title="Path parameters" />
  </Tab>
</Tabs>

<Tabs items={['V2', 'V1']} sync="admin-challenge-detail-version">
  <Tab>
    <ResponseBody
      def={GetAdminChallengeRouteV2}
      response="goodAdminChallengeV2"
      title="Response fields"
    />
  </Tab>
  <Tab>
    <ResponseBody
      def={GetAdminChallengeRoute}
      response="goodAdminChallenge"
      title="Response fields"
    />
  </Tab>
</Tabs>

V1 challenge entries do not include `hidden`, `releaseTime`, `instancerConfig`, `adminBotConfig`, or file `size`.
