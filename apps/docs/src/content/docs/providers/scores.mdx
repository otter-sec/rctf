---
title: Scoring providers
description: Choose and configure a dynamic scoring algorithm for your CTF.
---

import Callout from '@/components/callout.astro'
import ScoreDecayChart from '@/components/mdx/score-decay-chart.astro'

Scoring providers determine how challenge point values change as teams solve them. rCTF includes six scoring algorithms.

## Configuration

```yaml
scoreProvider:
  name: scores/classic # Default
```

Each challenge defines a point range via `[31mpoints.min[0m{:ansi}` and `[31mpoints.max[0m{:ansi}` in its data. The scoring provider calculates the current point value based on the number of solves and other context.

## Score context

All scoring providers receive a context object with:

| Field                   | Description                                                               |
| ----------------------- | ------------------------------------------------------------------------- |
| `[31mminPoints[0m{:ansi}`      | Minimum points for the challenge (floor)                                  |
| `[31mmaxPoints[0m{:ansi}`      | Maximum points for the challenge (ceiling, when 0 solves)                 |
| `[31msolves[0m{:ansi}`         | Current number of solves                                                  |
| `[31mmaxSolves[0m{:ansi}`      | Maximum solves across all challenges (used by `[32mscores/legacy[0m{:ansi}`)     |
| `[31meventStartTime[0m{:ansi}` | Competition start time (used by `[32mscores/jammy[0m{:ansi}`)                    |
| `[31meventEndTime[0m{:ansi}`   | Competition end time (used by `[32mscores/jammy[0m{:ansi}`)                      |
| `[31mfirstSolveTime[0m{:ansi}` | Time of the first solve for the challenge (used by `[32mscores/jammy[0m{:ansi}`) |

## Providers

Charts use a sample challenge with `[31mminPoints[0m: [32m100[0m{:ansi}` and `[31mmaxPoints[0m: [32m500[0m{:ansi}`. Solve-based providers show the full provider family with the current tab highlighted; `[32mscores/jammy[0m{:ansi}` uses its own time-based curve.

<Tabs items={['scores/classic', 'scores/sekai', 'scores/steep', 'scores/jammy', 'scores/genni', 'scores/legacy']}>
<Tab>

<ScoreDecayChart focus='classic' />

The **default** scoring algorithm. Uses a logistic decay function that decreases points as more teams solve the challenge.

```yaml
scoreProvider:
  name: scores/classic
```

Challenges start at `[31mmaxPoints[0m{:ansi}` with zero solves and decay toward `[31mminPoints[0m{:ansi}` as the solve count increases. The decay curve is moderate, suitable for most CTFs.

</Tab>
<Tab>

<ScoreDecayChart focus='sekai' />

Logarithmic decay with tuned constants (gradient=10, decay=60). Produces a gentler curve than `[32mscores/classic[0m{:ansi}`.

```yaml
scoreProvider:
  name: scores/sekai
```

</Tab>
<Tab>

<ScoreDecayChart focus='steep' />

Similar to `[32mscores/classic[0m{:ansi}` but with a steeper decay curve. Points drop more aggressively with early solves, making challenges differentiate faster.

```yaml
scoreProvider:
  name: scores/steep
```

</Tab>
<Tab>

<ScoreDecayChart type='time' />

Time-based scoring that values challenges by when they were first solved. Unlike other providers, this one considers the first solve time relative to the event duration rather than how many teams solved the challenge.

```yaml
scoreProvider:
  name: scores/jammy
  options:
    maximumScoreTime: 0.8 # Optional, default 0.8
```

| Option                    | Default    | Description                                                                                                                       |
| ------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------- |
| `[31mmaximumScoreTime[0m{:ansi}` | `0.8{:ts}` | Fraction of event duration used to ramp from minimum to maximum points. First solves after this threshold receive maximum points. |

<Callout variant="note">

This provider requires `[31mstartTime[0m{:ansi}` and `[31mendTime[0m{:ansi}` to be set in the configuration. It uses the first solve time to anchor the scoring curve.

</Callout>

</Tab>
<Tab>

<ScoreDecayChart focus='genni' />

Binary scoring: returns `[31mmaxPoints[0m{:ansi}` if the challenge has 0 to 2 solves, and `0{:ts}` points otherwise. Useful for challenges that should not contribute to the score once widely solved.

```yaml
scoreProvider:
  name: scores/genni
```

</Tab>
<Tab>

<ScoreDecayChart focus='legacy' />

Hyperbolic tangent-based decay that normalizes against the maximum solve count across all challenges. This was the original rCTF scoring algorithm.

```yaml
scoreProvider:
  name: scores/legacy
```

<Callout variant="note">

This provider requires `[31mmaxSolves[0m{:ansi}` (the maximum solve count across all challenges) to properly normalize the curve. It may produce unexpected results if challenge solve counts vary widely.

</Callout>

</Tab>
</Tabs>
