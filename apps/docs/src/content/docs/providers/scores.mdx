---
title: Scoring providers
description: Choose and configure a dynamic scoring algorithm for your CTF.
---

import Callout from '@/components/callout.astro'
import ScoreDecayChart from '@/components/mdx/score-decay-chart.astro'

Scoring providers determine how challenge point values change as teams solve them. rCTF includes six scoring algorithms.

## Configuration

```yaml
scoreProvider:
  name: scores/classic # Default
```

Each challenge defines a point range via `points.min{:yaml}` and `points.max{:yaml}` in its data. The scoring provider calculates the current point value based on the number of solves and other context.

## Score context

All scoring providers receive a context object with:

| Field                   | Description                                                               |
| ----------------------- | ------------------------------------------------------------------------- |
| `minPoints{:yaml}`      | Minimum points for the challenge (floor)                                  |
| `maxPoints{:yaml}`      | Maximum points for the challenge (ceiling, when 0 solves)                 |
| `solves{:yaml}`         | Current number of solves                                                  |
| `maxSolves{:yaml}`      | Maximum solves across all challenges (used by `scores/legacy{:yaml}`)     |
| `eventStartTime{:yaml}` | Competition start time (used by `scores/jammy{:yaml}`)                    |
| `eventEndTime{:yaml}`   | Competition end time (used by `scores/jammy{:yaml}`)                      |
| `firstSolveTime{:yaml}` | Time of the first solve for the challenge (used by `scores/jammy{:yaml}`) |

## Providers

Charts use a sample challenge with `minPoints: 100{:yaml}` and `maxPoints: 500{:yaml}`. Solve-based providers show the full provider family with the current tab highlighted; `scores/jammy{:yaml}` uses its own time-based curve.

<Tabs items={['scores/classic', 'scores/sekai', 'scores/steep', 'scores/jammy', 'scores/genni', 'scores/legacy']}>
<Tab>

The **default** scoring algorithm. Uses a logistic decay function that decreases points as more teams solve the challenge.

```yaml
scoreProvider:
  name: scores/classic
```

Challenges start at `maxPoints{:yaml}` with zero solves and decay toward `minPoints{:yaml}` as the solve count increases. The decay curve is moderate, suitable for most CTFs.

<ScoreDecayChart focus="classic" />

</Tab>
<Tab>

Logarithmic decay with tuned constants (gradient=10, decay=60). Produces a gentler curve than `scores/classic{:yaml}`.

```yaml
scoreProvider:
  name: scores/sekai
```

<ScoreDecayChart focus="sekai" />

</Tab>
<Tab>

Similar to `scores/classic{:yaml}` but with a steeper decay curve. Points drop more aggressively with early solves, making challenges differentiate faster.

```yaml
scoreProvider:
  name: scores/steep
```

<ScoreDecayChart focus="steep" />

</Tab>
<Tab>

Time-based scoring that values challenges by when they were first solved. Unlike other providers, this one considers the first solve time relative to the event duration rather than how many teams solved the challenge.

```yaml
scoreProvider:
  name: scores/jammy
  options:
    maximumScoreTime: 0.8 # Optional, default 0.8
```

| Option                    | Default    | Description                                                                                                     |
| ------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------- |
| `maximumScoreTime{:yaml}` | `0.8{:ts}` | Fraction of event duration used to ramp from minimum to maximum points. First solves after this threshold receive maximum points. |

<Callout variant="note">

This provider requires `startTime{:yaml}` and `endTime{:yaml}` to be set in the configuration. It uses the first solve time to anchor the scoring curve.

</Callout>

<ScoreDecayChart type="time" />

</Tab>
<Tab>

Binary scoring: returns `maxPoints{:yaml}` if the challenge has 0 to 2 solves, and `0{:ts}` points otherwise. Useful for challenges that should not contribute to the score once widely solved.

```yaml
scoreProvider:
  name: scores/genni
```

<ScoreDecayChart focus="genni" />

</Tab>
<Tab>

Hyperbolic tangent-based decay that normalizes against the maximum solve count across all challenges. This was the original rCTF scoring algorithm.

```yaml
scoreProvider:
  name: scores/legacy
```

<Callout variant="note">

This provider requires `maxSolves{:yaml}` (the maximum solve count across all challenges) to properly normalize the curve. It may produce unexpected results if challenge solve counts vary widely.

</Callout>

<ScoreDecayChart focus="legacy" />

</Tab>
</Tabs>
