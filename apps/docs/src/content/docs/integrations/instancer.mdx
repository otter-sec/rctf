---
title: Instancer
description: Configure per-team challenge instances with Docker or Kubernetes.
---

The instancer integration creates one isolated challenge service per team. rCTF owns the user-facing lifecycle, while an instancer provider owns the compute backend that creates, reads, extends, and deletes the running instance.

Instanced challenges are useful when teams need private mutable state, a dedicated service process, or a target that should disappear after a timeout.

<Callout variant='warning' title='Hostile workloads'>
  Treat challenge images as hostile. Set resource limits, keep containers or pods unprivileged, and
  avoid mounting host paths unless the challenge explicitly needs them.
</Callout>

## Architecture

The instancer path has three layers:

<Steps>
<Step title="Select a provider">

The rCTF API loads `<red>instancerProvider</red>` from `rctf.d/`. The provider validates challenge configs and translates rCTF lifecycle calls into Docker or Kubernetes operations.

</Step>
<Step title="Configure a challenge">

Each instanced challenge stores `<red>instancerConfig</red>` in challenge data. The common fields define the stable challenge integration ID, timeout, exposed endpoints, and provider-specific config.

</Step>
<Step title="Run participant instances">

Participants create, extend, inspect, and delete their instance from the challenge page. rCTF returns provider status and endpoints in the same order as the challenge's `<red>expose</red>` array.

</Step>
</Steps>

## Provider configuration

The `<red>instancerProvider</red>` config selects one provider in `rctf.d/`:

<Tabs items={['Docker', 'Kubernetes']}>
<Tab>

`<green>instancer/docker-instancer</green>` talks to the bundled Docker instancer or any compatible tiny-instancer API:

```yaml title="rctf.d/instancer.yaml"
instancerProvider:
  name: instancer/docker-instancer
  options:
    apiUrl: http://tiny-instancer:1337
    authToken: <shared-secret>
```

| Field or variable | Purpose |
| --- | --- |
| `<red>instancerProvider.options.apiUrl</red>` | Base URL of the Docker instancer API from the rCTF API process. |
| `<red>instancerProvider.options.authToken</red>` | Shared token sent as `<red>rctfAuthToken</red>` in lifecycle requests. |
| `TINY_INSTANCER_API_URL{:sh}` | Environment override for `<red>apiUrl</red>`. |
| `TINY_INSTANCER_AUTH_TOKEN{:sh}` | Environment override for `<red>authToken</red>`. |

</Tab>
<Tab>

`<green>instancer/k8s-instancer</green>` creates cluster-scoped `ChallengeInstance` custom resources:

```yaml title="rctf.d/instancer.yaml"
instancerProvider:
  name: instancer/k8s-instancer
  options:
    apiUrl: https://k8s.example.com
    authToken: <service-account-token>
    caCertificate: |
      -----BEGIN CERTIFICATE-----
      ...
      -----END CERTIFICATE-----
```

| Field or variable | Purpose |
| --- | --- |
| `<red>instancerProvider.options.apiUrl</red>` | Kubernetes API server URL. |
| `<red>instancerProvider.options.authToken</red>` | Bearer token for a service account that can create, get, patch, and delete `ChallengeInstance` resources. |
| `<red>instancerProvider.options.caCertificate</red>` | Kubernetes API CA certificate. This is required by the provider. |
| `K8S_INSTANCER_API_URL{:sh}` | Environment override for `<red>apiUrl</red>`. |
| `K8S_INSTANCER_AUTH_TOKEN{:sh}` | Environment override for `<red>authToken</red>`. |
| `K8S_INSTANCER_CA_CERTIFICATE{:sh}` | Environment override for `<red>caCertificate</red>`. |

</Tab>
</Tabs>

## Challenge configuration

Each instanced challenge needs `<red>instancerConfig</red>`:

```yaml title="challenge.yaml"
instancerConfig:
  challengeIntegrationId: web-demo
  timeoutMilliseconds: 600000
  extendable: true
  config:
    # Provider-specific config.
  expose:
    - kind: https
      hostPrefix: web-demo
      containerName: app
      containerPort: 8080
      shouldDisplay: true
      title: Challenge
```

| Field | Purpose |
| --- | --- |
| `<red>challengeIntegrationId</red>` | Stable ID used in Docker labels, Kubernetes resource names, and lifecycle requests. It should stay stable after launch. |
| `<red>timeoutMilliseconds</red>` | Instance lifetime for create and extend operations. |
| `<red>extendable</red>` | Enables participant-initiated extension when set to `true{:ts}`. |
| `<red>config</red>` | Provider-specific service or pod configuration. |
| `<red>expose</red>` | Public endpoints rCTF should display or keep hidden for internal routing. |

The `<red>expose</red>` entries map public endpoints to a service container or pod:

| Field | Purpose |
| --- | --- |
| `<red>kind</red>` | Endpoint kind. The values are `<green>http</green>`, `<green>https</green>`, `<green>tcp</green>`, and `<green>tcp-ssl</green>`. |
| `<red>hostPrefix</red>` | Prefix used when generating the per-instance hostname. |
| `<red>containerName</red>` | Docker service name or Kubernetes pod name receiving traffic. |
| `<red>containerPort</red>` | Port inside the service container or Kubernetes service. |
| `<red>shouldDisplay</red>` | Controls whether the endpoint is shown to participants. Hidden endpoints still exist. |
| `<red>title</red>` | Optional display label. |

## Endpoint kinds

Endpoint support is provider-specific:

| Kind | Docker behavior | Kubernetes behavior |
| --- | --- | --- |
| `<green>http</green>` | Traefik HTTP router on the configured HTTP port. The default is `80{:ts}`. | Traefik `IngressRoute` on the `web` entrypoint. The public port is `80{:ts}`. |
| `<green>https</green>` | Traefik HTTPS router on the configured HTTPS port. The default is `443{:ts}`. | Traefik `IngressRoute` on the `websecure` entrypoint plus an HTTP redirect route. The public port is `443{:ts}`. |
| `<green>tcp</green>` | Rewritten to `<green>tcp-ssl</green>` because Traefik needs SNI routing. | Returned as `unsupported-by-instancer` with port `0{:ts}`. Kubernetes configs should use `<green>tcp-ssl</green>`. |
| `<green>tcp-ssl</green>` | Traefik TCP router with TLS and HostSNI on the configured TCP port. The default is `1337{:ts}`. | Traefik `IngressRouteTCP` with TLS and HostSNI on port `1337{:ts}`. |

## Docker instancer

The Docker instancer is a Python FastAPI service that manages Docker containers, Docker networks, Docker volumes, Redis instance locks, Redis expirations, and Traefik labels.

The bundled deployment files live under `deploy/docker-instancer`:

<FileTree>
  <Dir name='deploy'>
    <Dir name='docker-instancer'>
      <File name='compose.yml' comment='Traefik, Redis, and tiny-instancer service' />
      <File name='.env.example' comment='Required environment variables' />
      <File name='Dockerfile' comment='Python 3.14 runtime image' />
      <Dir name='conf' comment='Traefik static and dynamic config' />
      <Dir name='certs' comment='TLS certificate mount point' />
    </Dir>
  </Dir>
</FileTree>

The Compose stack exposes Traefik on `80{:ts}`, `443{:ts}`, and `1337{:ts}`. The instancer API itself is bound to `127.0.0.1:12237` on the host and to `tiny-instancer:1337` inside the `rctf_network` Docker network.

The environment file requires shared auth, Redis, and public instance host settings:

```sh title="deploy/docker-instancer/.env"
DEV_ENV=false
BIND_PORT=1337
WEB_WORKERS=1
USE_PROXY_HEADERS=true

AUTH_TOKEN=<shared-secret>
INSTANCES_HOST=instancer.example.com

REDIS_HOST=cache
REDIS_PORT_NUMBER=6379
REDIS_PASSWORD=<redis-password>

TRAEFIK_PERMANENT_REDIRECT_MIDDLEWARE_NAME=permanent-https-redirect@file
```

The Docker instancer stack starts from the repository root:

```sh
$ docker compose -f deploy/docker-instancer/compose.yml up -d
```

The same token belongs in rCTF:

```yaml title="rctf.d/instancer.yaml"
instancerProvider:
  name: instancer/docker-instancer
  options:
    apiUrl: http://tiny-instancer:1337
    authToken: <shared-secret>
```

When rCTF is outside the `rctf_network` Docker network, the host-mapped API URL is:

```yaml title="rctf.d/instancer.yaml"
instancerProvider:
  name: instancer/docker-instancer
  options:
    apiUrl: http://127.0.0.1:12237
    authToken: <shared-secret>
```

The Traefik TLS config expects these files:

<FileTree>
  <Dir name='deploy'>
    <Dir name='docker-instancer'>
      <Dir name='certs'>
        <File name='fullchain.pem' comment='Certificate chain' />
        <File name='privkey.pem' comment='Private key' />
      </Dir>
    </Dir>
  </Dir>
</FileTree>

### Docker challenge config

The Docker provider accepts a Docker Compose-like object under `<red>config</red>`:

```yaml title="challenge.yaml"
instancerConfig:
  challengeIntegrationId: web-demo
  timeoutMilliseconds: 600000
  extendable: true
  config:
    services:
      app:
        image: ghcr.io/example/web-demo:latest
        environment:
          FLAG: flag{example}
        networks:
          - internal
        expose:
          - '8080'
        read_only: true
        mem_limit: 128m
        cpus: 0.5
        pids_limit: 128
    networks:
      internal:
        internal: true
  expose:
    - kind: https
      hostPrefix: web-demo
      containerName: app
      containerPort: 8080
      title: Challenge
```

The top-level Docker config supports `<red>services</red>`, `<red>networks</red>`, and `<red>volumes</red>`. At least one service is required after defaults are applied.

Each service supports these field groups:

| Fields | Purpose |
| --- | --- |
| `<red>image</red>` | Docker image. This is required for each explicit service. |
| `<red>hostname</red>`, `<red>environment</red>`, `<red>command</red>`, `<red>entrypoint</red>`, `<red>working_dir</red>`, `<red>user</red>` | Process and runtime metadata. |
| `<red>networks</red>`, `<red>network_mode</red>`, `<red>dns</red>`, `<red>dns_opt</red>`, `<red>dns_search</red>`, `<red>extra_hosts</red>`, `<red>expose</red>` | Network configuration. |
| `<red>volumes</red>`, `<red>tmpfs</red>`, `<red>shm_size</red>`, `<red>read_only</red>` | Filesystem configuration. |
| `<red>privileged</red>`, `<red>security_opt</red>`, `<red>cap_add</red>`, `<red>cap_drop</red>` | Container privilege controls. |
| `<red>mem_limit</red>`, `<red>cpus</red>`, `<red>pids_limit</red>`, `<red>ulimits</red>`, `<red>sysctls</red>` | Resource and kernel limits. |
| `<red>healthcheck</red>`, `<red>labels</red>`, `<red>restart</red>` | Health, metadata, and restart behavior. |

Explicit services default to a read-only root filesystem, `<green>no-new-privileges</green>`, dropped Linux capabilities, `6m` memory, `1.0` CPU, `64` PIDs, and `nofile` soft and hard limits of `1024`.

Services may only reference networks declared under `<red>config.networks</red>`. Named volume mounts may only reference volumes declared under `<red>config.volumes</red>`. Absolute and relative bind-style mount sources are not checked against the volume map.

## Kubernetes instancer

<Callout variant='tip' title='Dedicated deployment guide'>
  See [Kubernetes instancer](/integrations/k8s-instancer/) for the full Terraform deployment
  walkthrough, including GKE provisioning, ACME wildcard certificates, RBAC, network policies, and
  local Kind development.
</Callout>

The Kubernetes provider creates `ChallengeInstance` custom resources. The controller watches those resources and creates a namespace, network policies, deployments, services, and Traefik routes for each participant instance.

The controller creates namespaces named from the challenge integration ID and team ID:

```text
inst-<challenge-integration-id>-<team-id>
```

Each namespace is deleted when the `ChallengeInstance` expires or is deleted. The controller uses a finalizer so the namespace is removed before the custom resource disappears.

### Kubernetes deployment

The Terraform example under `deploy/terraform/instancer/example` provisions the expected GKE, Traefik, controller, DNS, certificate, and rCTF service-account pieces.

The Kubernetes module reads a generated controller installer from `apps/k8s-controller/dist/install.yaml`. Build and push the controller image, then generate the installer:

```sh
$ cd apps/k8s-controller
$ make build-installer IMG=registry.example.com/rctf-instancer-controller:tag
```

The Terraform example applies from `deploy/terraform/instancer/example`:

```sh
$ terraform init
$ terraform apply
```

The Terraform outputs map directly to the rCTF provider config:

| Terraform output                | rCTF option                |
| ------------------------------- | -------------------------- |
| `rctf_instancer_api_url`        | `<red>apiUrl</red>`        |
| `rctf_instancer_auth_token`     | `<red>authToken</red>`     |
| `rctf_instancer_ca_certificate` | `<red>caCertificate</red>` |

For local controller development, the controller README uses Kind:

```sh
$ kind create cluster --name rctf --config kind-config.yaml
$ make install
$ make run ARGS="-instancer-host instancer.test"
```

### Kubernetes challenge config

Kubernetes challenge config uses `<red>pods</red>` under the provider-specific `<red>config</red>` object:

```yaml title="challenge.yaml"
instancerConfig:
  challengeIntegrationId: web-demo
  timeoutMilliseconds: 600000
  extendable: true
  config:
    pods:
      - name: app
        egress: false
        ports:
          - name: http
            port: 80
            targetPort: 8080
        spec:
          automountServiceAccountToken: false
          containers:
            - name: app
              image: ghcr.io/example/web-demo:latest
              env:
                - name: FLAG
                  value: flag{example}
              ports:
                - containerPort: 8080
              resources:
                requests:
                  memory: 100Mi
                  cpu: 75m
                limits:
                  memory: 250Mi
                  cpu: 100m
              securityContext:
                runAsNonRoot: true
                readOnlyRootFilesystem: true
                allowPrivilegeEscalation: false
                capabilities:
                  drop:
                    - ALL
  expose:
    - kind: https
      hostPrefix: web-demo
      containerName: app
      containerPort: 80
      title: Challenge
```

Each `<red>pods[]</red>` item has these fields:

| Field | Purpose |
| --- | --- |
| `<red>name</red>` | Name used for the Deployment and Service. `<red>expose[].containerName</red>` must match this value. |
| `<red>ports</red>` | Kubernetes Service ports. These are not container ports. |
| `<red>spec</red>` | Kubernetes PodSpec used as the Deployment template. |
| `<red>egress</red>` | Enables outbound public internet egress for this pod when set to `true{:ts}`. |

The provider validates a focused subset of the Kubernetes PodSpec. Containers support image, command, args, working directory, ports, env, resources, restart policy, volume mounts, liveness and readiness probes, image pull policy, and security context. Pod specs support volumes, init containers, restart policy, termination grace period, service account token mounting, pod security context, hostname, DNS config, and resources.

### Kubernetes networking

The controller creates these network policies for every instance namespace:

| Policy | Behavior |
| --- | --- |
| `isolate-namespace` | Allows pod traffic inside the instance namespace and DNS egress to `kube-system`. |
| `ingress-traefik` | Allows Traefik in the `traefik` namespace to reach exposed pods. |
| `egress` | Allows pods labeled for egress to reach public IPv4 addresses. Private, CGNAT, and link-local ranges are excluded. |

The `<red>egress</red>` flag is per pod. Challenges that should not reach the internet should keep it `false{:ts}`.

## Lifecycle and API behavior

Participant lifecycle routes are under `<black>/api/v2/integrations/challs/:id/instance</black>`:

| Method                   | Action                                                |
| ------------------------ | ----------------------------------------------------- |
| `<cyan>GET</cyan>`       | Returns current status, time left, and endpoints.     |
| `<magenta>PUT</magenta>` | Creates an instance.                                  |
| `<orange>PATCH</orange>` | Extends an instance when the challenge is extendable. |
| `<red>DELETE</red>`      | Stops an instance.                                    |

The returned status is one of `<green>stopped</green>`, `<green>starting</green>`, `<green>running</green>`, `<green>stopping</green>`, or `<green>errored</green>`. Docker returns `<green>stopping</green>` only through rCTF's shared response schema, while Kubernetes returns it when a custom resource has a deletion timestamp.

Captcha can protect create and extend actions:

```yaml title="rctf.d/captcha.yaml"
captcha:
  protectedEndpoints:
    - instancerStart
    - instancerExtend
```

## Schema and troubleshooting

Admins can retrieve the active provider's JSON schema from:

```text
/api/v2/admin/instancer/schema
```

The schema is useful for challenge import tools and generated challenge data validation.

<Callout variant='note' title='Challenge repository tooling'>
  [Konata](https://github.com/project-sekai-ctf/konata) is a CTF challenge management tool with rCTF
  support on its roadmap, including challenge syncing, Docker image publishing, and Kubernetes
  manifest deployment. The archived [DiceCTF Quals 2026
  challenges](https://github.com/dicegang/dicectf-quals-2026-challenges) repository is a useful
  reference for Konata-style challenge configuration.
</Callout>

Common checks:

- A `<red>404 badEndpoint</red>` response means no instancer provider is configured.
- A `<red>400 badInstancerConfig</red>` response means the challenge config failed provider validation.
- A Docker `<green>tcp</green>` expose becomes `<green>tcp-ssl</green>` because raw TCP cannot be SNI-routed by host.
- A Kubernetes `<green>tcp</green>` expose is currently unsupported. The configured kind should be `<green>tcp-ssl</green>`.
- Kubernetes requires `<red>caCertificate</red>` even when the API server certificate is otherwise trusted by the host.
- The admin bot `<red>requireInstancerInstancesRunning</red>` check only confirms an instance is running before queueing the job. Challenge handlers should still tolerate a stopped or expired instance.
