---
title: Configuration
description: Complete reference for all rCTF configuration options including file-based config and environment variables.
---

import Callout from '@/components/callout.astro'

rCTF is configured through YAML or JSON files in a `rctf.d/` directory and optional environment variable overrides.

## Configuration loading

Configuration is loaded from a directory named `rctf.d/`. The loader searches upward from the `packages/config/` directory, or you can specify a path via the `RCTF_CONF_PATH{:sh}` environment variable.

All files in the directory (`.yaml`, `.yml`, or `.json`) are loaded **alphabetically** and deep-merged in order. This allows you to split configuration across multiple files for organization:

<FileTree>
  <Dir name='rctf.d'>
    <File name='01-base.yaml' comment='Core settings' />
    <File name='02-providers.yaml' comment='Provider configuration' />
    <File name='03-overrides.yaml' comment='Environment-specific overrides' />
  </Dir>
</FileTree>

Environment variables are applied last, overriding any file-based values.

## Environment variables

The following environment variables are supported. They override values from config files.

### Core

| Variable                  | Type          | Description                                              |
| ------------------------- | ------------- | -------------------------------------------------------- |
| `RCTF_NAME{:sh}`          | `string{:ts}` | CTF display name                                         |
| `RCTF_ORIGIN{:sh}`        | `string{:ts}` | CTF origin URL (e.g., `https://ctf.example.com`)         |
| `RCTF_TOKEN_KEY{:sh}`     | `string{:ts}` | Base64-encoded 32-byte key for token encryption          |
| `RCTF_INSTANCE_TYPE{:sh}` | `string{:ts}` | `all{:yaml}`, `frontend{:yaml}`, or `leaderboard{:yaml}` |
| `RCTF_CONF_PATH{:sh}`     | `string{:ts}` | Path to config directory (overrides search)              |

### Database

| Variable                      | Type           | Description                                       |
| ----------------------------- | -------------- | ------------------------------------------------- |
| `RCTF_DATABASE_URL{:sh}`      | `string{:ts}`  | PostgreSQL connection string                      |
| `RCTF_DATABASE_HOST{:sh}`     | `string{:ts}`  | PostgreSQL host (if not using URL)                |
| `RCTF_DATABASE_PORT{:sh}`     | `integer{:ts}` | PostgreSQL port                                   |
| `RCTF_DATABASE_USERNAME{:sh}` | `string{:ts}`  | PostgreSQL user                                   |
| `RCTF_DATABASE_PASSWORD{:sh}` | `string{:ts}`  | PostgreSQL password                               |
| `RCTF_DATABASE_DATABASE{:sh}` | `string{:ts}`  | PostgreSQL database name                          |
| `RCTF_DATABASE_MIGRATE{:sh}`  | `string{:ts}`  | `before{:yaml}`, `only{:yaml}`, or `never{:yaml}` |
| `RCTF_REDIS_URL{:sh}`         | `string{:ts}`  | Redis connection string                           |
| `RCTF_REDIS_HOST{:sh}`        | `string{:ts}`  | Redis host (if not using URL)                     |
| `RCTF_REDIS_PORT{:sh}`        | `integer{:ts}` | Redis port                                        |
| `RCTF_REDIS_PASSWORD{:sh}`    | `string{:ts}`  | Redis password                                    |
| `RCTF_REDIS_DATABASE{:sh}`    | `integer{:ts}` | Redis database number                             |

### Timing and auth

| Variable                          | Type           | Description                                |
| --------------------------------- | -------------- | ------------------------------------------ |
| `RCTF_START_TIME{:sh}`            | `integer{:ts}` | Competition start time (Unix milliseconds) |
| `RCTF_END_TIME{:sh}`              | `integer{:ts}` | Competition end time (Unix milliseconds)   |
| `RCTF_LOGIN_TIMEOUT{:sh}`         | `integer{:ts}` | Verification token expiry in milliseconds  |
| `RCTF_USER_MEMBERS{:sh}`          | `boolean{:ts}` | Enable team members feature                |
| `RCTF_CTFTIME_CLIENT_ID{:sh}`     | `string{:ts}`  | CTFtime OAuth client ID                    |
| `RCTF_CTFTIME_CLIENT_SECRET{:sh}` | `string{:ts}`  | CTFtime OAuth client secret                |

### UI and meta

| Variable                     | Type          | Description                                                        |
| ---------------------------- | ------------- | ------------------------------------------------------------------ |
| `RCTF_HOME_CONTENT{:sh}`     | `string{:ts}` | Home page markdown content                                         |
| `RCTF_FAVICON_URL{:sh}`      | `string{:ts}` | Favicon URL                                                        |
| `RCTF_META_DESCRIPTION{:sh}` | `string{:ts}` | Meta description                                                   |
| `RCTF_IMAGE_URL{:sh}`        | `string{:ts}` | Meta image URL                                                     |
| `RCTF_GLOBAL_SITE_TAG{:sh}`  | `string{:ts}` | Google Analytics tag (deprecated, use `analytics.provider{:yaml}`) |

### Email

| Variable                   | Type          | Description                 |
| -------------------------- | ------------- | --------------------------- |
| `RCTF_EMAIL_FROM{:sh}`     | `string{:ts}` | Email sender address        |
| `RCTF_EMAIL_LOGO_URL{:sh}` | `string{:ts}` | Logo URL in email templates |

### Leaderboard

| Variable                                  | Type           | Description                      |
| ----------------------------------------- | -------------- | -------------------------------- |
| `RCTF_LEADERBOARD_MAX_LIMIT{:sh}`         | `integer{:ts}` | Max teams per leaderboard page   |
| `RCTF_LEADERBOARD_MAX_OFFSET{:sh}`        | `integer{:ts}` | Max leaderboard offset           |
| `RCTF_LEADERBOARD_UPDATE_INTERVAL{:sh}`   | `integer{:ts}` | Leaderboard recalc interval (ms) |
| `RCTF_LEADERBOARD_GRAPH_MAX_TEAMS{:sh}`   | `integer{:ts}` | Max teams on score graph         |
| `RCTF_LEADERBOARD_GRAPH_SAMPLE_TIME{:sh}` | `integer{:ts}` | Graph sample interval (ms)       |

<Callout variant="note">

Boolean environment variables accept `true{:yaml}`, `yes{:yaml}`, `y{:yaml}`, or `1{:yaml}` as truthy values. Anything else is treated as false.

</Callout>
## Configuration reference

Below is the complete reference of all configuration options available in `rctf.d/` files.

### Core settings

```yaml
ctfName: My CTF # CTF display name (required)
origin: https://ctf.example.com # Public origin URL (required)
tokenKey: <base64-32-byte-key> # Token encryption key (required)
instanceType: all # all | frontend | leaderboard
```

| Field                 | Type          | Default      | Description                                                                                                                              |
| --------------------- | ------------- | ------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `ctfName{:yaml}`      | `string{:ts}` | -            | Display name of your CTF                                                                                                                 |
| `origin{:yaml}`       | `string{:ts}` | -            | Public URL of your CTF (no trailing slash)                                                                                               |
| `tokenKey{:yaml}`     | `string{:ts}` | -            | Base64-encoded 32-byte key for AES-GCM token encryption                                                                                  |
| `instanceType{:yaml}` | `string{:ts}` | `all{:yaml}` | Controls which components run: `all{:yaml}` (API + leaderboard worker), `frontend{:yaml}` (API only), `leaderboard{:yaml}` (worker only) |

<Callout variant="warning">

The `tokenKey{:yaml}` is critical for security. All authentication tokens are encrypted with this key. If it is lost or changed, all existing tokens become invalid and users will need to re-authenticate.

</Callout>
### Database

```yaml
database:
  sql: postgres://user:password@localhost:5432/rctf
  redis: redis://localhost:6379
  migrate: never
```

The `sql{:yaml}` field accepts either a connection string or an object:

```yaml
database:
  sql:
    host: localhost
    port: 5432
    user: rctf
    password: secret
    database: rctf
    maxPoolSize: 100 # Default: 100
    idleTimeout: 30000 # Default: 30000 (ms)
    connectTimeout: 3000 # Default: 3000 (ms)
```

The `redis{:yaml}` field accepts either a connection string or an object:

```yaml
database:
  redis:
    host: localhost
    port: 6379
    password: secret # Optional
    database: 0 # Optional, default 0
```

| Field                     | Type                           | Default        | Description                                                                                               |
| ------------------------- | ------------------------------ | -------------- | --------------------------------------------------------------------------------------------------------- |
| `database.sql{:yaml}`     | `string{:ts}` \| `object{:ts}` | -              | PostgreSQL connection (required)                                                                          |
| `database.redis{:yaml}`   | `string{:ts}` \| `object{:ts}` | -              | Redis connection (required)                                                                               |
| `database.migrate{:yaml}` | `string{:ts}`                  | `never{:yaml}` | `before{:yaml}` runs migrations on startup, `only{:yaml}` runs migrations and exits, `never{:yaml}` skips |

### Timing

```yaml
startTime: 1735689600000 # January 1, 2025 00:00 UTC
endTime: 1735776000000 # January 2, 2025 00:00 UTC
```

| Field              | Type          | Default | Description                                            |
| ------------------ | ------------- | ------- | ------------------------------------------------------ |
| `startTime{:yaml}` | `number{:ts}` | -       | Competition start time in Unix milliseconds (required) |
| `endTime{:yaml}`   | `number{:ts}` | -       | Competition end time in Unix milliseconds (required)   |

<Callout variant="tip">

To convert a date to Unix milliseconds: `date -d "2025-01-01T00:00:00Z" +%s000{:sh}` or use `new Date('2025-01-01T00:00:00Z').getTime()` in JavaScript.

</Callout>
### Divisions

```yaml
divisions:
  open: Open
  student: Student
defaultDivision: open
divisionACLs:
  - match: domain
    value: edu
    divisions: [student, open]
  - match: any
    value: ''
    divisions: [open]
```

| Field                    | Type          | Default            | Description                                   |
| ------------------------ | ------------- | ------------------ | --------------------------------------------- |
| `divisions{:yaml}`       | `object{:ts}` | `{ open: "Open" }` | Map of division ID to display name            |
| `defaultDivision{:yaml}` | `string{:ts}` | -                  | Default division for new users (optional)     |
| `divisionACLs{:yaml}`    | `array{:ts}`  | -                  | Access control rules for divisions (optional) |

#### Division ACLs

ACLs control which divisions a user can register for based on their email. Each ACL entry has:

| Field              | Description                                                                                   |
| ------------------ | --------------------------------------------------------------------------------------------- |
| `match{:yaml}`     | Match type: `domain{:yaml}`, `email{:yaml}`, `regex{:yaml}`, or `any{:yaml}`                  |
| `value{:yaml}`     | Value to match against (domain suffix, exact email, regex pattern, or empty for `any{:yaml}`) |
| `divisions{:yaml}` | Array of division IDs this rule grants access to                                              |

ACL rules are evaluated in order. The first matching rule determines the user's allowed divisions.

<Callout variant="note">

Division ACLs only apply when an email provider is configured. If CTFtime authentication is enabled, ACLs are bypassed entirely - all divisions become available.

</Callout>
### Authentication

```yaml
registrationsEnabled: true
userMembers: true
maxMembers: 50
loginTimeout: 3600000
ctftime:
  clientId: '12345'
  clientSecret: your-secret
```

| Field                         | Type           | Default        | Description                                                |
| ----------------------------- | -------------- | -------------- | ---------------------------------------------------------- |
| `registrationsEnabled{:yaml}` | `boolean{:ts}` | `true{:ts}`    | Whether new registrations are allowed                      |
| `userMembers{:yaml}`          | `boolean{:ts}` | `true{:ts}`    | Enable team members feature                                |
| `maxMembers{:yaml}`           | `number{:ts}`  | `50{:ts}`      | Maximum members per team                                   |
| `loginTimeout{:yaml}`         | `number{:ts}`  | `3600000{:ts}` | Verification/CTFtime token expiry in milliseconds (1 hour) |
| `ctftime.clientId{:yaml}`     | `string{:ts}`  | -              | CTFtime OAuth client ID (numeric string)                   |
| `ctftime.clientSecret{:yaml}` | `string{:ts}`  | -              | CTFtime OAuth client secret                                |

<Callout variant="note">

Auth tokens (used for logging in) never expire. Only verification and CTFtime tokens expire according to `loginTimeout{:yaml}`.

</Callout>
### Captcha

```yaml
captcha:
  provider:
    name: captcha/turnstile
    options:
      siteKey: your-site-key
      secretKey: your-secret-key
  protectedEndpoints:
    - register
    - recover
```

| Field                               | Type          | Default | Description                                                |
| ----------------------------------- | ------------- | ------- | ---------------------------------------------------------- |
| `captcha.provider{:yaml}`           | `object{:ts}` | -       | Captcha provider config (`name{:yaml}` + `options{:yaml}`) |
| `captcha.protectedEndpoints{:yaml}` | `array{:ts}`  | -       | List of actions requiring captcha                          |

Available captcha actions: `register{:yaml}`, `recover{:yaml}`, `setEmail{:yaml}`, `instancerStart{:yaml}`, `instancerExtend{:yaml}`, `avatarUpload{:yaml}`, `adminBotSubmit{:yaml}`.

See [Captcha Providers](/providers/captcha) for provider-specific configuration.

### Providers

```yaml
uploadProvider:
  name: uploads/s3
  options:
    bucketName: my-bucket
    awsKeyId: AKIAIOSFODNN7EXAMPLE
    awsKeySecret: wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
    awsRegion: us-east-1
scoreProvider:
  name: scores/classic
instancerProvider:
  name: instancer/docker-instancer
  options:
    apiUrl: http://localhost:8000
    authToken: secret
```

| Field                      | Type          | Default                      | Description                             |
| -------------------------- | ------------- | ---------------------------- | --------------------------------------- |
| `uploadProvider{:yaml}`    | `object{:ts}` | `{ name: "uploads/local" }`  | File upload provider                    |
| `scoreProvider{:yaml}`     | `object{:ts}` | `{ name: "scores/classic" }` | Scoring algorithm provider              |
| `instancerProvider{:yaml}` | `object{:ts}` | -                            | Challenge instancer provider (optional) |

See the [Providers](/providers) section for detailed configuration of each provider.

### Admin bot

```yaml
adminBot:
  provider:
    name: admin-bot/rctf-js
    options: {}
  maxLogsPerUserChallenge: 5
```

| Field                                     | Type           | Default  | Description                                   |
| ----------------------------------------- | -------------- | -------- | --------------------------------------------- |
| `adminBot.provider{:yaml}`                | `object{:ts}`  | -        | Admin bot provider config                     |
| `adminBot.maxLogsPerUserChallenge{:yaml}` | `integer{:ts}` | `5{:ts}` | Max stored log entries per user per challenge |

### Email

```yaml
email:
  provider:
    name: emails/smtp
    options:
      smtpUrl: smtp://user:pass@mail.example.com:587
  from: noreply@example.com
  logoUrl: https://example.com/logo.png
```

| Field                   | Type          | Default | Description                                      |
| ----------------------- | ------------- | ------- | ------------------------------------------------ |
| `email.provider{:yaml}` | `object{:ts}` | -       | Email provider config                            |
| `email.from{:yaml}`     | `string{:ts}` | -       | Sender email address (required if email enabled) |
| `email.logoUrl{:yaml}`  | `string{:ts}` | -       | Logo URL for email templates (optional)          |

<Callout variant="note">

Email is required for account registration when CTFtime auth is not configured. It is also needed for account recovery and email-based division ACLs.

</Callout>
See [Email Providers](/providers/emails) for provider-specific options.

### UI

```yaml
homeContent: |
  Welcome to My CTF!

  **Markdown** is supported.
sponsors:
  - name: Sponsor Name
    icon: https://example.com/sponsor.png
    description: Sponsor description
    url: https://sponsor.com
meta:
  description: A cool CTF competition
  imageUrl: https://example.com/banner.png
faviconUrl: https://example.com/favicon.ico
logoLightUrl: https://example.com/logo-light.svg
logoDarkUrl: https://example.com/logo-dark.svg
flagFormatPlaceholder: 'flag{[\x20-\x7e]+}'
```

| Field                          | Type          | Default                               | Description                                                                         |
| ------------------------------ | ------------- | ------------------------------------- | ----------------------------------------------------------------------------------- |
| `homeContent{:yaml}`           | `string{:ts}` | `"Home content. Markdown supported."` | Home page content (Markdown)                                                        |
| `sponsors{:yaml}`              | `array{:ts}`  | `[]`                                  | List of sponsors (`name{:yaml}`, `icon{:yaml}`, `description{:yaml}`, `url{:yaml}`) |
| `meta.description{:yaml}`      | `string{:ts}` | `"rCTF event description"`            | HTML meta description                                                               |
| `meta.imageUrl{:yaml}`         | `string{:ts}` | `""`                                  | HTML meta image URL                                                                 |
| `faviconUrl{:yaml}`            | `string{:ts}` | rCTF default                          | Favicon URL                                                                         |
| `logoLightUrl{:yaml}`          | `string{:ts}` | `""`                                  | Logo for light mode                                                                 |
| `logoDarkUrl{:yaml}`           | `string{:ts}` | `""`                                  | Logo for dark mode                                                                  |
| `flagFormatPlaceholder{:yaml}` | `string{:ts}` | `"flag{[\\x20-\\x7e]+}"`              | Flag format hint shown to participants                                              |

<Callout variant="tip">

Most UI settings and the competition timeline (`ctfName{:yaml}`, `startTime{:yaml}`, `endTime{:yaml}`, `homeContent{:yaml}`, `sponsors{:yaml}`, `meta{:yaml}`, `faviconUrl{:yaml}`, `logoLightUrl{:yaml}`, `logoDarkUrl{:yaml}`) can also be changed at runtime through the admin settings API without restarting the server.

</Callout>
### Analytics

```yaml
analytics:
  provider:
    name: analytics/plausible
    options:
      domain: ctf.example.com
      src: https://plausible.io/js/script.js
```

| Field                       | Type          | Default | Description                                                                                                 |
| --------------------------- | ------------- | ------- | ----------------------------------------------------------------------------------------------------------- |
| `analytics.provider{:yaml}` | `object{:ts}` | -       | Analytics provider (`analytics/google{:yaml}`, `analytics/cloudflare{:yaml}`, `analytics/plausible{:yaml}`) |

### Limits

```yaml
maxAvatarSize: 1048576 # 1 MB
leaderboard:
  maxLimit: 100
  maxOffset: 4294967296
  updateInterval: 30000
  graphMaxTeams: 10
  graphSampleTime: 1800000
  graphWithListLimit: 100
```

| Field                                   | Type          | Default                | Description                                         |
| --------------------------------------- | ------------- | ---------------------- | --------------------------------------------------- |
| `maxAvatarSize{:yaml}`                  | `number{:ts}` | `1048576{:ts}` (1 MB)  | Maximum avatar upload size in bytes                 |
| `leaderboard.maxLimit{:yaml}`           | `number{:ts}` | `100{:ts}`             | Max teams returned per leaderboard request          |
| `leaderboard.maxOffset{:yaml}`          | `number{:ts}` | `4294967296{:ts}`      | Max pagination offset                               |
| `leaderboard.updateInterval{:yaml}`     | `number{:ts}` | `30000{:ts}` (30s)     | Leaderboard recalculation interval in ms            |
| `leaderboard.graphMaxTeams{:yaml}`      | `number{:ts}` | `10{:ts}`              | Max teams displayed on score graph                  |
| `leaderboard.graphSampleTime{:yaml}`    | `number{:ts}` | `1800000{:ts}` (30min) | Time between graph data points in ms                |
| `leaderboard.graphWithListLimit{:yaml}` | `number{:ts}` | `100{:ts}`             | Max teams for combined leaderboard + graph endpoint |

### Moderation

```yaml
avatarsModeration:
  provider:
    name: moderation/openai
    options:
      apiKey: sk-...
  allowOnInternalError: true
```

| Field                                           | Type           | Default     | Description                                 |
| ----------------------------------------------- | -------------- | ----------- | ------------------------------------------- |
| `avatarsModeration.provider{:yaml}`             | `object{:ts}`  | -           | Moderation provider for avatar uploads      |
| `avatarsModeration.allowOnInternalError{:yaml}` | `boolean{:ts}` | `true{:ts}` | Allow avatar upload if moderation API fails |

See [Moderation Providers](/providers/moderation) for details.

### Proxy

```yaml
proxy:
  cloudflare: true
  trust: false
```

| Field                     | Type                                                                | Default      | Description                                                                                                                                          |
| ------------------------- | ------------------------------------------------------------------- | ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `proxy.cloudflare{:yaml}` | `boolean{:ts}`                                                      | `false{:ts}` | Trust Cloudflare `CF-Connecting-IP` header for client IP                                                                                             |
| `proxy.trust{:yaml}`      | `boolean{:ts}` \| `string{:ts}` \| `string[]{:ts}` \| `number{:ts}` | `false{:ts}` | Proxy trust setting for `X-Forwarded-For`. `true{:ts}` trusts all, a number trusts the first N hops, a string or array specifies trusted CIDR ranges |

<Callout variant="warning">

Set `proxy.cloudflare{:yaml}` to `true{:ts}` if your rCTF instance is behind Cloudflare. This ensures correct client IP extraction for rate limiting and logging. When using a different reverse proxy, configure `proxy.trust{:yaml}` instead.

</Callout>
### Blood bot

```yaml
bloodBot:
  bloodsCount: 3
  destinations:
    - provider:
        name: messages/discord
        options:
          url: https://discord.com/api/webhooks/...
    - provider:
        name: messages/telegram
        options:
          botToken: '123456:ABC-DEF...'
          chatId: '-1001234567890'
```

| Field                                            | Type          | Default  | Description                             |
| ------------------------------------------------ | ------------- | -------- | --------------------------------------- |
| `bloodBot.bloodsCount{:yaml}`                    | `number{:ts}` | `1{:ts}` | Number of blood tiers to announce (1-3) |
| `bloodBot.destinations{:yaml}`                   | `array{:ts}`  | -        | At least one destination (required)     |
| `bloodBot.destinations[].provider{:yaml}`        | `object{:ts}` | -        | Messages provider config                |
| `bloodBot.destinations[].messageTemplate{:yaml}` | `string{:ts}` | -        | Custom message template (optional)      |

Available template variables: `{{teamName}}`, `{{teamUrl}}`, `{{bloodNumSentence}}`, `{{challengeCategory}}`, `{{challengeName}}`.

See [Blood Bot](/integrations/bloodbot) for more details.

## Complete example

```yaml title="rctf.d/01-base.yaml"
ctfName: Example CTF 2025
origin: https://ctf.example.com
tokenKey: dGhpcyBpcyBhIGJhc2U2NCBrZXkgZXhhbXBsZSE=

database:
  sql: postgres://rctf:password@localhost:5432/rctf
  redis: redis://localhost:6379
  migrate: before

startTime: 1735689600000
endTime: 1735776000000

divisions:
  open: Open
  student: Student
defaultDivision: open
divisionACLs:
  - match: domain
    value: edu
    divisions: [student, open]
  - match: any
    value: ''
    divisions: [open]

captcha:
  provider:
    name: captcha/turnstile
    options:
      siteKey: 0x4AAAAAAA...
      secretKey: 0x4AAAAAAA...
  protectedEndpoints:
    - register

email:
  provider:
    name: emails/smtp
    options:
      smtpUrl: smtp://user:pass@mail.example.com:587
  from: noreply@example.com

uploadProvider:
  name: uploads/s3
  options:
    bucketName: ctf-uploads
    awsKeyId: AKIAIOSFODNN7EXAMPLE
    awsKeySecret: wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
    awsRegion: us-east-1

proxy:
  cloudflare: true

bloodBot:
  bloodsCount: 3
  destinations:
    - provider:
        name: messages/discord
        options:
          url: https://discord.com/api/webhooks/123/abc
```
