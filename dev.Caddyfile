# docker run --rm --network host -v "$(pwd)/dev.Caddyfile:/etc/caddy/Caddyfile:ro" caddy:2.8-alpine
:8000 {
  @api path /api/*

  handle @api {
    reverse_proxy localhost:3000
  }

  handle {
    reverse_proxy localhost:8080
  }
}