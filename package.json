{
  "name": "rctf",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --elide-lines=0 --filter '@rctf/api' dev",
    "dev:api": "bun run --elide-lines=0 --filter '@rctf/api' dev",
    "dev:web": "bun run --elide-lines=0 --filter '@rctf/web' dev",
    "build": "bun run --filter '*' build",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "prettier": "prettier --write \"**/*.{ts,svelte,html,json}\" --ignore-path .gitignore",
    "typecheck": "bun run --filter '*' typecheck",
    "db:generate": "bun run --filter '@rctf/db' generate",
    "db:migrate": "bun run --filter '@rctf/db' migrate",
    "db:pull": "bun run --filter '@rctf/db' pull"
  },
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "^4.7.0",
    "@types/bun": "latest",
    "prettier": "^3.6.2",
    "prettier-plugin-svelte": "^3.4.0",
    "prettier-plugin-tailwindcss": "^0.7.1"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "prettier": {
    "plugins": [
      "prettier-plugin-tailwindcss",
      "prettier-plugin-svelte",
      "@ianvs/prettier-plugin-sort-imports"
    ],
    "tabWidth": 2,
    "useTabs": false,
    "semi": false,
    "singleQuote": true,
    "jsxSingleQuote": true,
    "quoteProps": "as-needed",
    "trailingComma": "es5",
    "bracketSpacing": true,
    "bracketSameLine": false,
    "arrowParens": "avoid"
  }
}
